# Roadmap: review backlog and citation chain depth design

Written 2026-09-08 on `main` at `b921de8`. This is the single place that
says what comes next. Every session that finishes an item ticks it here
and commits the tick with the work. Read this file first; then read only
the entry or spec the current item points to.

Inputs (do not re-derive them):

- Backlog: `docs/superpowers/handoffs/2026-09-08-review-backlog.md`
  (items B1 to B6, D1, D2, F1 to F3, working rules at the bottom).
- Design: `docs/design_handoff_citation_chain_depth/README.md` (option
  6a; open `#6a` in `Citation Chain Depth.dc.html`).
- Ledger of finished plans: `.superpowers/sdd/progress.md`.

## Why this order

The design's option 6a removes the Explore view, moves seeds into the
rail and replaces the focus projection with an additive Scope model
(collections stay visible, hops add neighbours, rows filter by
checkbox). That is the answer to D1 and makes D2 moot, so neither is
fixed on the current focus model. B4's labels depend on which toolbar
entries survive, so they are decided inside the design brainstorm. B6
is fixed before hop fetching is built on the same seed pipeline.

## Stage 1: independent bugs (backlog entries, one commit each)

Each is a feature branch, `npm run check`, fast-forward to main,
`npm run build`, push. Small enough for one session each, or several in
one session.

- [x] B1 native prompt dialogs (`Services.prompt`)
- [x] B2 empty saved-graph list on first open
- [x] B3 tab rename does not rename the saved graph
- [x] B5 spec wording only (`2026-09-07-selection-sync-design.md`)
- [x] F2 wheel zoom sensitivity
- [x] F1 node menu: open PDF or open online
- [x] B6 external seed then Add to Zotero fails (reproduce with the
      error console, then `superpowers:systematic-debugging`)

## Stage 2: design, layer 1, rail Scope and toolbar reduction

Process: `superpowers:brainstorming`, then spec in
`docs/superpowers/specs/`, plan in `docs/superpowers/plans/`, then
subagent-driven development. Feature branch `graph-scope-rail`.

Scope of the layer: Scope section in `graphKeyRail.ts` (count line,
presets, Seeds rows with hover, remove, Add seed, Collections checkbox
tree with "Not in Zotero"), toolbar reduced to Filter, Similar, Export,
Refresh, search; Explore identity, history arrows, Explore settings,
Add Node and Seeds popover removed; focus projection retired in favour
of the additive model. Resolves D1 and D2. Decides B4's item-menu
labels ("New Graph from N items", "Show in", "Refresh").

Brainstorm must state explicitly which behaviours from
`2026-09-05-explore-toolbar-controls-design.md`,
`2026-09-07-seed-entry-points-design.md` and
`2026-09-07-refresh-preserves-state` (Explore settings persistence,
seed popover, sticky selects) survive, are moved into the rail, or are
deleted, so the spec does not silently drop approved work.

- [x] brainstorm and spec (`2026-09-08-graph-scope-rail-design.md`)
- [x] plan (`2026-09-08-graph-scope-rail.md`)
- [x] implemented, reviewed, merged, XPI built, pushed
- [x] manual walk-through by the user (2026-09-09: all nine checks pass as
      written; the walk found eighteen new items, B12 to B22, F5 to F9 and
      D3 to D5)

## Stage 3: design, layer 2, citation hops on demand

Depends on Stage 2 and on B6. Hop rows Seeds, Hop 1 to Hop 6 with
`{shown}/{available}` or `not fetched`; Fetch hop N button with
progress; hop toggles; presets Full, Balanced, Core; persisted state
(`chainDepth`, `loadedDepth`, `hopEnabled`, `preset`). Data: hop k+1 is
the union of `citedBy` over visible hop k papers through the existing
provider pipeline, cached, with full parent lists per node.

- [x] brainstorm and spec (`2026-09-12-citation-hops-design.md`, two adversarial reviews folded in)
- [x] plan (`docs/superpowers/plans/2026-09-12-citation-hops.md`, 15 tasks)
- [x] implemented, reviewed, merged, XPI built, pushed
- [ ] manual walk-through by the user

## Stage 4: design, layer 3, citation floor and shared citers

Depends on Stage 3. Floor line on the plot with drag handle and outline
rendering below the floor; Shared citers Off, Dim, Only with the
grading formula and Key tiers; visibility order of evaluation as a pure
function with unit tests; label routing through `graphLabelBudget.ts`.

- [ ] brainstorm and spec
- [ ] plan
- [ ] implemented, reviewed, merged, XPI built, pushed
- [ ] manual walk-through by the user

## Design items from the Stage 2 walk-through

These came out of the 2026-09-09 batch. D3 is the one to do first: B12 is a
symptom of it, and fixing B12 alone would be repainted by whatever D3 decides.
Each is `superpowers:brainstorming`, then a spec, then a plan.

- [x] D3 the colour system: folder colours, seed colours, the metric ramp and
      the in-library ring are four palettes drawn from two lists, and they
      collide. Decides what a seed's mark means and how the palettes stay
      apart. ~~B12~~, subsumed by D3 rather than fixed on its own.
- [x] D6 the folder regions read as faceted polylines, and their offset does
      not follow the zoom. The outline is a centripetal Catmull-Rom → cubic
      Bézier fit inside `regionPathFor`, and the falloff radius tightens past
      the fit zoom only, on a quantised 12% bucket that joins the contour
      cache key. The second half reverses D3's data-space decision knowingly;
      the reason is recorded in D6's spec, in D3's spec and in
      `graphFolderRegion.ts`'s docstring.
- [x] D4 graph templates: named bundles of view settings with icons, so a
      beginner does not have to build a view out of the gear panel. Read the
      rail's existing presets line first. Built 2026-09-12 as
      `2026-09-12-graph-views.md`; three views greyed until Stage 3, Stage 4
      and reading state.
- [ ] D5 the logo: one mark on both themes that says meristem, replacing the
      blue installer icon and the white UI icon.

## Next: B28, the D3 regression

Found in the user's walk of D3's batch on 2026-09-10. It comes before
everything below, including Stage 3: a non-empty region selection blanks the
plot, so a graph opened from a folder shows no nodes at all, and five of D3's
eight checks cannot be walked until it is fixed. Reproduce with the Error
Console before touching anything — `superpowers:systematic-debugging`.

- [x] B28 selecting a folder blanks the plot; a folder graph opens blank.
      `Path2D` is a DOM constructor and the plugin's bundle has none on its
      scope; the region path now comes from the canvas's own window, as
      `ResizeObserver` already did. The suite missed it because its own
      doubles supplied `Path2D` on the global.
- [x] B24 `regionsForRenderer` and `seedColorsFor` allocated a swatch as a side
      effect of reading one; now `peek` on a ledger store, with the allocating
      `ensureSwatchesFor` only on the paths that reach `notifyStateChange`.
- [x] B23 a deleted collection's ID lingered in a saved graph's `regions`;
      `applyState` now keeps only the IDs the snapshot's collections still
      have, the way `toggleRow` drops an unticked one.
- [x] B29 switching light → dark leaves the plot background light. Two
      causes. The 2026-09-10 sighting was B28 from another side: a region
      throw had latched `canvasError`, and a latched renderer skips the
      redraw a flip asks for. Reproducing on the fixed build found a second:
      Light → Automatic on a dark OS resolves at the pref write, when the
      chrome's `prefers-color-scheme` still says light, and nothing fires
      when it catches up. Automatic now reads the OS through
      `-moz-system-dark-theme`, which is right at the write. Region and seed
      colours, read off the theme only when pushed, now re-read on the
      renderer's `onThemeChange`
- [x] B30 a node outside the open folder left Zotero's list on whatever it
      had selected before; Zotero's `selectItems` returns 0 for an unlisted
      row and moves nothing, so the binding now clears the list and absorbs
      the echo (pairs with F10; the same seam from the other side)

## Filler, any time after Stage 1

- [ ] F3 standards without a main author (short brainstorm, small plan)
- [ ] B7 New Graph on an empty library fails silently (decision: open the
      empty state, or tell the user; backlog entry B7)
- [x] B8 the saved-graph dialogs are cramped and Delete paints white
      edges. Decided 2026-09-08: keep the native dialogs. No work
      follows; the entry records why the look is what it is.
- [ ] F4 show the selected paper's abstract in the graph (decision: which
      surface carries it; found walking the 2026-09-08 batch)
- [x] B9 a Semantic Scholar key makes the plugin request faster than its
      plan allows. Do this one **before the key is entered**, not after:
      today's keyless path is inside the limit, and pasting a key is what
      breaks it. Same shape applies to the OpenAlex key.
- [x] B13 "+ Add seed" is unreadable on the dark theme; B14 the seed row's ×
      grows an off-centre ellipse; B16 the Open list centres its names; B17
      Tools repeats the plugin icon on every row; B21 File is fourth in the
      plot toolbar and belongs first. Five small surface fixes; one branch
      carries them all with B19 (`surface-fixes`, awaiting the manual walk).
- [x] B27 an out-of-range swatch index painted `strokeStyle = undefined`, so
      canvas kept the previous region's colour; clamped at the colour read.
- [x] B25 the category swatch ledger lived on the renderer and died with it;
      now `categorySwatches` on the saved state, restored into the renderer.
- [x] B22 Advanced is collapsed; open it. Decided 2026-09-09 that the rows
      stay, dashes included — a dash is evidence, and F9 is what fixes it.
      Done 2026-09-12: the `<details>` opens by default and keeps its twisty,
      so the section can still be folded away. One check in the manual batch
- [ ] F9 a tool for adding a DOI, starting with the one the plugin already
      holds: a resolved match carries the DOI and is never written back to the
      item. Needs the unconfirmed-match case designed, not skipped.
- [ ] F8 nothing says why a paper has no metrics. A paper the providers cannot
      name gets none, and the exact-title fallback is what decides that for a
      paper with no DOI
- [ ] B15 adding a seed from the search panel takes two clicks. Still there on
      2026-09-13, reported again unprompted during the manual walk: "we still
      have the plus icon '+' to add the item as seed instead of just clicking
      the item in the list to add it". The row itself should be the target
- [ ] B18 the view fits after it renders, so the graph jumps
- [x] B19 a folder's context menu should read "Create a new graph" (on
      `surface-fixes` with the five above)
- [ ] B20 New Graph from a detached window gives no feedback (low priority,
      the user said so)
- [ ] F5 the node's context menu should offer what the detail pane offers
      (and Refresh vs Update connections should say which scope it acts on)
- [ ] F6 seed a paper that is not in Zotero into a new graph
- [ ] F7 a new graph should tick what it was made from. Restated by the user
      2026-09-13, mid-walk, when a graph made from two selected papers opened
      its gallery over the whole library — "How do you want to look at these
      69 papers?": "The intended action is that when I open the two papers as
      seed, I DON'T want to see the whole Zotero library with it. I want to
      see the two papers as seeds only." So this is not only about folders:
      a seeded graph made from selected items should start scoped to those
      seeds, with the library admitted by a deliberate tick rather than by
      default. It also decides what the gallery's heading counts, since that
      N is the visible count and today it is the library's
- [x] B11 a one-row library selection leaves the detail pane's placeholder
      up ("view 15" in the Zotero suite). The pane was right and the case was
      wrong: the node is selected and the paper is shown, but a paper with no
      metrics carries a placeholder of its own, so the case's body-wide
      `.cm-placeholder` query read a selected paper as an empty pane.
- [ ] F10 select nodes in the graph and have Zotero follow: no multi-select
      on the canvas, no graph → list direction, and the one-row ring and the
      multi-row opacity are two visual languages for one idea
- [ ] F11 clicking a Seeds row should select that seed, and selecting a seed
      node should light its row
- [ ] F12 selecting a folder in Zotero activates its region in the graph
      (needs a brainstorm: add or replace, tick or not, which window, the cap)
- [ ] F13 show a paper's full title in the graph; today it ellipsises
- [x] B31 the rail does not show subfolders clearly. Asked 2026-09-11: nesting
      is invisible and the cascade is illegible; widened by the user to a
      redesign of the folder rows. Spec approved the same day:
      `2026-09-11-scope-rail-folders-design.md` (indent guides, a filled
      square in place of the checkbox, Zotero's selected-row fill for a
      region). Built the same day on `scope-rail-folders`, plan
      `2026-09-11-scope-rail-folders.md`; the handlers did not change, the
      marks did. Three checks in the manual batch
- [x] F14 remove the four-folder region cap; the user decided it on
      2026-09-11. The user wanted more colours rather than wrapping or a
      higher cap, so the categorical list is twelve per theme and a
      thirteenth folder takes the Other tone
- [x] B32 one deep purple region swatch does not read on the dark theme.
      It was the fourth dark seed `#442b87`, not a region swatch: 12
      lightness points off the paper. Now `#6c448c`, and the validator
      floors every data colour at 20 points from its paper
- [x] B33 two halos deform toward each other and meet in a point before
      they merge: the `1 - d²/R²` kernel has a hard edge. Now `(1 - d²/R²)²`
      with the support widened 1.31× so a lone disc keeps its size; the user
      picked it from a rendered comparison of three kernels
- [ ] B38 the border grows a tip toward a neighbouring paper just before the
      two merge (the level set sharpening into the saddle), while the fill,
      rounded by the 12 px dilation the border does not get, stays round.
      Deferred by the user on 2026-09-11 after three renderings: "nothing
      there looks better ... skip this one." Not to be reopened unless
      they do
- [x] B39 `--cm-surface-paper` and `--cm-ink-muted` are emitted by
      `graphThemeCustomProperties` and read by nothing since B31 replaced
      the selected row's paper background; dropped from `graphTheme.ts`. No
      unit test named the pair, and the theme fields stay, since the
      renderer still reads them. Found by B31's final review on 2026-09-11
- [x] B40 the rest of the emitter is the same shape as B39: of the tokens
      `graphThemeCustomProperties` writes, `graph.css` reads only
      `--cm-surface-panel`. The `--cm-ramp-*`, `--cm-category-*`,
      `--cm-edge-*`, `--cm-state-*`, `--cm-seed-*`, `--cm-surface-hairline`,
      `--cm-surface-grid`, `--cm-ink-primary` and `--cm-ink-emphasis`
      properties have never had a `var()` reader since 1595d22 introduced
      them; the canvas takes its colours from the theme object, and nothing
      reads them by name from JS. Two unit cases name the emitter (the ring
      token is published; both schemes emit the same names). Decision: drop
      all but the panel token, or keep the emitter as the bridge the header
      comment describes; the comment in `graph.css` is wrong either way.
      The user chose to drop them on 2026-09-12: the emitter publishes
      only `--cm-surface-panel`, one unit case pins that, and the header
      comment says so
- [x] B41 the gear writes the seeded appearance preference
      (`setFocusGraphAppearance`) but every open reads the global one
      (`getFocusGraphAppearance` has no caller), so a gear change on a seeded
      graph is lost on reopen. Found by D4's adversarial review on 2026-09-12.
      Fixed 2026-09-12: `initialGraphAppearance(seeded)` in
      `citationPreferences.ts` reads the focus record for a graph that opens
      seeded (a request with seeds, or a saved recipe with them) and the
      global one otherwise; `renderGraphView` asks for it that way. Unit
      cases pin both branches and the citation-sequence fallback. Consequence
      worth knowing: a new seeded graph opens on the citation-sequence x axis
      (the focus default the record has always described) unless the gear
      was changed on a seeded graph before, which is the pre-b421a00 look
- [ ] B34 panning a 300+ paper folder at maximum zoom lags; measure
      `drawRegions` before touching it. Later, the user said so
- [x] B35 the item menu's "fetches online" hint goes, and "New Graph from N
      items" should say the items become seeds; ask for the wording. The
      user chose "New Graph with N seeds" and dropped every hint, the
      collection entries' "library only" included
- [ ] B36 one "Uncaught (in promise) undefined" in the Error Console, no
      stack; second sighting of an undefined rejection. Name it when it
      comes back
- [x] B10 no exponential backoff; a 429 gets one fixed 1.5 s retry. The user
      ticked Semantic Scholar's backoff commitment on 2026-09-08, so this
      makes the code match it. Pairs with B9; one small plan covers both.
- [x] B42 an unparseable saved-graph state opens as an empty graph and the
      next autosave overwrites it (savedGraphService.ts falls back to
      emptyGraphViewState); an older build reading a version 4 row loses the
      recipe. Make an unparseable state read-only. Found by D4's final review
      2026-09-12. Fixed 2026-09-12: the store's `load` reports `readOnly`
      when the state did not parse, the window opens such a graph without
      binding it to the row (no autosave, no write on close, Save turns into
      Save as…) and keeps a notice in the toolbar status until Save as…
      gives it a row of its own. New copy: "This graph was saved by a newer
      version of Meristema, so it is read-only here; Save as… keeps changes."
      Unit cases in `savedGraphService.test.ts`; suite case in
      `graphViews.test.ts` writes a version 99 row raw, opens it from File ›
      Open, chooses Start blank and asserts the row is untouched after the
      autosave delay and after the tab closes
- [x] B43 the Scope rail suite's canvas node-menu walk (hover, then right-click
      hit-test) fails three cases together in roughly one run of four on the
      graph-views branch (logs kept 2026-09-12: runs 3, 8a, 8b); main at
      908d94a was 51/0 on three runs, so it has only been seen with D4.
      Diagnosed 2026-09-12 on the branch's re-review: the gallery is not the
      cause, and the walk's own timing is. Four more runs (9, 11 green at
      57/0; 10 red on an unrelated chip race in D4's own import case, since
      fixed; 12 red with the cascade) were run with the gallery's `show`
      guarded against repainting an unchanged heading, which did not stop it.
      The evidence the walk now prints settles it: on run 12 it made 37
      passes over the canvas in 20 s, was offered 3 papers by the hover
      tooltip, and never saw the entry — so the pointer hit-test works, and
      the right-click is what comes up empty. The walk adds every paper it
      hovers to a `tried` set and reads `.cm-node-menu` in the same turn as
      the `contextmenu` it dispatches, so a menu that opens a tick later is
      missed and that paper is never right-clicked again: 36 of the 37 passes
      could do nothing. The fix is in the walk — await the menu (or drop the
      paper from `tried` when no menu appeared) — not in the gallery or its
      blur. The blur A/B was not run: hover and right-click are dispatched
      straight at the canvas element, and the hover half demonstrably works,
      so an overlay cannot be what swallows the other half.
      Corrected and fixed 2026-09-12: that diagnosis did not survive the
      code. The menu opens synchronously inside the `contextmenu` handler,
      so nothing arrives "a tick later" and a poll could not have helped.
      The walk was instrumented instead (per right-click: the point, whether
      the renderer's hit test found a node, read off `defaultPrevented`; the
      menu's state; capture spies on the document and the canvas recording
      the coordinates the event carried; a pointerleave-then-rehover at the
      same point). Two red logs (`b43-run2.log`, `b42-run1.log` in the
      session scratchpad) agree: the walk's grid points were fractional
      (`box.left + 4` came to 182.8), the hover PointerEvent kept the
      fraction, and the `contextmenu` MouseEvent arrived at the truncated
      integer (182,302). The first grid point to enter a node's disc is at
      its fringe, so 0.8 px is enough for the right-click to miss what the
      hover found: every click had hit=false, the menu never opened, and the
      re-hover found the paper again. Whether the canvas box sat on a
      fractional pixel is what made it one run in four. The walk now starts
      its grid at the box's ceiling so both events test one whole pixel;
      the per-click record stays in the failure message. Four runs green
      after the fix (`b43-fix-run1..4.log`)
- [x] B44 the view gallery's cards break their layout: on a graph opened from
      a folder context menu, the gallery appears but the card text and some
      icons paint outside their card container. Found 2026-09-13 at the top
      of the manual walk, on the `43dfe4b` XPI: "The template selector shows
      up, but the cards layout is messed up. I can see the card containers,
      but the text is showing outside of it, as well as some icons." The
      D4 gallery check below is left open and unwalked; the cards are styled
      by `.cm-view-gallery-card` and its children in `addon/content/graph.css`
      (from 800), built in `src/services/graphViewsMenu.ts`. No diagnosis
      yet — filed during the walk, not chased. What two screenshots show
      (`B44_dark-large.png`, `B44_light-small.png`, kept on the user's
      Desktop and in the session scratchpad; "The bug happens in all
      combinations of theme and size"): the card chrome and the card content
      have come apart. The grid draws a row of three empty rounded
      containers and a second row of them beneath, while every card's icon,
      name, paragraph and note paint below and across those containers,
      each card's text overlapping its neighbour's — "Overview" over "Who
      cites what?", "Folder map" over "Lets the papers settle…". The
      Start blank button and the Import view JSON… link paint on top of the
      third card's paragraph. Five views are laid out over three columns.
      Widened 2026-09-13 when the View dropdown was walked: the same breakage
      is there, so this is not the gallery's alone. "The views have a similar
      problem to the template cards. The description texts aren't inside the
      containers, they overflow." Both surfaces are built by
      `graphViewsMenu.ts` and both draw a row of icon, name and one-liner, so
      look for one shared rule rather than fixing them twice. The tutorial
      card, checked separately, is sound.
      Not cosmetic, on the same walk: in the dropdown the rows' hit areas are
      offset by a row from what is painted, so one view cannot be applied at
      all. "I literally cannot reach. The mouse jumps from 'Who cites whom'
      to 'Save current as view...'. The pointer hover on top of 'Folder map'
      but the selection shows as if it is on top of 'Who cites whom'." That
      is the same fault read from the other side — the layout boxes and the
      drawn text disagree — and it is the strongest clue to the cause, so
      chase it from there. Raise the priority accordingly: Folder map is
      unreachable from the View dropdown on this build, which also blocks
      the D4 Folder map check below — though not fatally: the user applied
      Folder map from the gallery instead and it worked. That narrows the
      fault usefully. Both surfaces overflow their containers, but only the
      dropdown's rows are mis-hit; the gallery's cards are clickable where
      they are drawn. So the overflow and the offset are two symptoms that do
      not always travel together, and whatever the dropdown does differently
      with its row boxes is where to look
      Diagnosed and fixed 2026-09-13. One cause, and it is not in the plugin's
      layout at all: Zotero's own `chrome://zotero-platform/content/zotero.css`
      ships `:is(button:where(:not(.btn, [type=checkbox], [type=radio])), …)
{ height: 28px }`. `:where()` costs no specificity, so that selector
      weighs a single type selector and loses to every rule the plugin writes
      — but a rule only wins the properties it declares, and the plugin's base
      button rule (`paperDetail.css:52`) declared `min-height` and never
      `height`. So every plugin `<button>` in a tab was pinned to exactly 28px
      whatever it held. The gallery's cards need 130–197px and the dropdown's
      rows 141–200px, so both painted their text below a 28px box. That also
      explains the asymmetry recorded above, which was the right clue read the
      wrong way round: a card's box is 28px tall and _starts_ where the card is
      drawn, so a click near the card's top still lands on it, while the
      dropdown stacks five 28px boxes under five taller painted rows, so the
      boxes drift a row out of step and Folder map cannot be reached. The fix
      is `height: auto` on that base rule, which restores every plugin button
      at once while `min-height: 28px` keeps the floor a one-line button wants.
      Guarded by `test/zotero/hostButtonHeight.test.ts`, which measures the
      gallery built by its own `createViewGallery` and the dropdown's rows
      against their `scrollHeight`.
      Why no existing test caught it, which is the more useful finding: the
      visual harness opens the graph in its own XUL window (`openViewStage`,
      `visualHarness.ts:537`), and that window never loads
      `zotero-platform/content/zotero.css`. The whole suite was green against a
      cascade the product never runs in. Anything guarding the plugin against a
      rule of Zotero's own has to be measured in the main window, which is what
      the new file does
- [ ] B45 the seed search panel's rows have lost the clear space between the
      title and the author line. Reported 2026-09-13 during the manual walk:
      "The title and the author lines are too close." The Stage 2 check
      ticked that spacing as passing on 2026-09-08, so this is a regression
      or a re-judgement on a later build; walk it with B15, whose fix touches
      the same rows
- [x] B46 the rail's "+ Add seed" paints a dark background on hover, and it is
      too tight around the text. Reported 2026-09-13 during the manual walk:
      "the button gets a dark background effect that is too tight. Ideally
      that button should not have a background effect triggered by the hover,
      but it should have a similar effect as the little 'x' when we want to
      remove a seed." The × is the treatment B14 settled: no box of any
      shape, the glyph goes from muted to full ink and a little heavier. Give
      "+ Add seed" the same. Widened later the same day: the progress line's
      Stop and Resume links do it too — "the Resume and Stop buttons have the
      same issue as I mentioned to the '+ Add seed', an ugly background
      effect when hovering." So this is every text link in the rail, not one
      button; fix them as a family rather than one at a time.
      Fixed 2026-09-14, option A of three the user was shown on a canvas with
      live hover (underline; full ink; underline and heavier): no box, an
      underline and nothing else. Measured first in Zotero's main window with
      the hover held by `InspectorUtils`: "+ Add seed", Stop and Show all all
      painted the base rule's accent tint, `color(srgb 0.124 0.14 0.21)` in
      dark, on a 28px box with a 6px radius. The cause is B59's again, read
      from the hover side: `.meristema-root button:hover:not(:disabled)` is
      (0,3,1) and the links' own class-only hover was (0,3,0), so it only
      ever won the underline. The family was one wider than the entry
      thought: the tutorial card's "Don't show for this view again"
      (`cm-link-button`) wore the same box and had no underline at all. The
      four now share one rule that carries the tag, as the × does since B14.
      Guarded in `test/zotero/hostButtonHeight.test.ts` (red: four boxes, and
      the card's link not underlined)
- [x] B59 a selected folder row's accent blue is drawn around its label, not
      behind it, on both themes. Found 2026-09-13 walking B31's
      selected-region check, which it fails: "The container that has the
      folder name and the number of items has a background color, and
      because of that, I can't see the blue selection under it. So the
      selection is displayed around that container, but not through/behind
      it." One cause covers both halves of what the user saw. The row body
      is a `<button>` (`graphKeyRail.ts:478-483`), and
      `.cm-scope-row-body` (`graph.css:1575-1594`) is the only button rule
      in that stylesheet that does not carry the `.meristema-root` prefix
      its neighbours use — compare `.meristema-root button.cm-scope-seed-remove`
      and `.meristema-root .cm-scope-add-seed`, which carry it precisely to
      beat Zotero's own button styling. So the button keeps Zotero's native
      background, which is opaque and sits over
      `.cm-scope-row-selected`'s accent fill on the parent row, and Zotero's
      native hover too: with the pointer parked on the plot "the item has
      its own background color", and on hover "I get a blue-hover ONLY on
      the container that doesn't get the blue when selected" — a blue where
      the plugin's own rule asks for a neutral 8% tint. The fix is one
      prefix, but check the whole file for other unprefixed rules on
      elements that are buttons. Related but not the same as B46, which is
      about hover backgrounds the design does not want on text links; this
      one is an unwanted background the design already tried to remove
      Fixed 2026-09-13, alongside B44. The entry's diagnosis was right about
      the missing prefix and one step out about who was winning: the opaque
      background is not Zotero's native button styling but the plugin's _own_
      base rule, `.meristema-root button` at (0,1,1) in `paperDetail.css`,
      which a bare `.cm-scope-row-body` at (0,1,0) loses to. Measured before
      the fix, the row body computed `color(srgb 0.127 0.123 0.151)` — the base
      rule's `--cm-surface-raised`, opaque, over the selected row's accent
      fill. The hover is the same story one notch up: the base's
      `.meristema-root button:hover:not(:disabled)` is (0,3,1), which is why a
      neutral 8% tint came out accent blue. All three rules now read
      `.meristema-root button.cm-scope-row-body…`, the pattern
      `cm-scope-seed-remove` already uses; the hover pair ties the base at
      (0,3,1) and wins on sheet order, since graph.css's link is kept after
      paperDetail.css's. So B44 and B59 are the same _family_ — a plugin button
      wearing chrome it never asked for — but not one fix: B44 was a property
      no plugin rule declared, B59 a selector too weak to carry the ones it
      did. Guarded in `test/zotero/hostButtonHeight.test.ts`.
      The audit the entry asked for is done: every class the source puts on a
      `<button>`, checked against the base rule's properties. `cm-node-menu-item`
      looks weak but carries `!important` and is fine; the one real find is
      `cm-key-entry`, filed as B61
- [x] B60 clearing Zotero's list leaves the old row focused, so it still
      wears a dashed ring and still anchors the keyboard. Found 2026-09-13
      rewalking B30's original case, which otherwise passes: "it doesn't
      completelly clear the selection, it just doesn't show as selected, but
      there is skill a ghost dashed blue edge around the previous selected
      item. If you press up/down we can still navigate the selection in
      Zotero. So we don't have a fully unselected item." The mechanism is
      exact: `clearTree` (`zoteroSelectionSync.ts:173-180`) calls
      `selection.clearSelection()`, which empties the selection and leaves
      the tree's focused index untouched, and the focused index is what
      draws the dashed outline and what an arrow key moves from. The
      question the fix has to answer first, because it is not obvious: a
      tree with nothing focused makes the next arrow key start from the top,
      which is worse for a keyboard reader than a stale anchor. So the
      choice is between clearing the focus too, and keeping it but making it
      not look like a selection. Cosmetic in weight, but it is the one thing
      standing between B30's fix and the clean "the list clears" the check
      asks for.
      Closed 2026-09-14 without a code change, on the user's call: the state
      is Zotero's own. Read from the installed Zotero's
      `components/virtualized-table.js`, the entry's premise does not hold —
      `TreeSelection.focused` starts at 0 and has no "nothing focused" value,
      so clearing it would only move the ring to the top row, and ↓ from
      there selects the second row (`focused + 1`), skipping the first paper.
      The ring is the platform rule
      `.virtualized-table.multi-select:focus .row.focused:not(.selected)`, a
      dotted accent outline drawn only while the list holds keyboard focus,
      and Zotero reaches the identical state itself when a Ctrl+click
      deselects a row (`toggleSelect`). After B30's clear the selection is
      genuinely empty (`getSelectedItems` returns `[]`); the ring marks where
      the arrow keys start. Hiding it with a plugin rule was weighed and
      declined: it would hide that anchor, not remove it. One manual check
      compares the two states
- [ ] B47 during a hop fill the nodes move far more than the new data should
      move them, and some are drawn where their own values say they are not.
      Reported 2026-09-13 during the manual walk, on a seeded graph filled
      from a blank view: "As the hops load (starting with a blank view) the
      nodes jump too much after each fetch. I see papers with 0 citations in
      2026 that shows as high as 100 citations in the view, and suddenly goes
      to a negative place. It also varies the location in the year around
      2026." Two claims to separate when this is chased: the whole plot
      reshuffling on every landing (an axis rescale per fetch would do that,
      and B18's after-render fit is a neighbour), and a single paper's drawn
      position disagreeing with its citation count and its year, changing
      between fetches. The "No data" parking lane is ruled out: asked on
      2026-09-13, the user placed the jumping nodes elsewhere — "The 'No
      data' nodes sit close to the axis origins, the jumping ones that I
      mentioned are at the bleeding edge of most recent & most cited." That
      is the corner the plot is read from, and the user says why it matters:
      "one of the best features here is to show the most cited like a pareto
      front." So the defect is in how the outermost, most-cited papers are
      placed and replaced as hops land, not in the parking lane. No
      diagnosis yet — filed during the walk
- [ ] B48 a paper whose year is filled in after the graph is drawn stays in
      the No data state. Reported 2026-09-13 during the manual walk: "there
      was one item that had citations but not the year, after adding the year
      in the Zotero metadata it still shows as No data, close to the origin."
      So the graph is not picking up an edit to an item's metadata, or the
      per-paper value is cached past it. Whether the toolbar's Refresh clears
      it is the first thing to establish. Second observation from the same
      report, which may be its own defect: the rail explains No data as "a
      paper with no value for an axis is parked in a lane off the plot", but
      the user sees those papers "close to the axis origins" — a lane at the
      origin is not off the plot, and a paper parked there reads as a real
      low-low value
- [x] B49 the rail's counts group thousands with a comma (`1,557`, `of
2,726`) on a machine whose locale is de-CH, where the Swiss form is
      `1'557`. Seen 2026-09-13 during the manual walk, transcribed from the
      screen twice. `graphKeyRail.ts:140` and `graphScopeRailModel.ts:155`
      both format with `new Intl.NumberFormat(undefined, { useGrouping: true
})`, so they take the runtime's default locale — which means Zotero's
      JS locale is resolving to English, not the OS's de-CH, while the Zotero
      suite on this machine formats de-CH. Cosmetic and low priority, and
      arguably correct for an English UI; the question to settle is which
      locale the plugin should follow, Zotero's or the machine's.
      Closed 2026-09-14 without a code change, on the user's call: the
      plugin follows Zotero, because Zotero does exactly the same. Read from
      the installed `omni.ja`: the tag selector formats its counts with
      `new Intl.NumberFormat().format(num)`, and the note box's word count
      and the full-text index statistics with `toLocaleString()` — no locale
      argument anywhere, so all of them take the app's default locale, as
      the rail does. The profile sets no `intl.locale.requested` and no
      `intl.regional_prefs.use_os_locales`, so on this English install every
      count in the window, Zotero's and the plugin's, groups as `1,557`, and
      a change of Zotero's language moves them together. The `1'557` this
      machine prints comes from Node, where the unit tests run on the OS
      locale, which is why those tests derive the grouped digits from the
      formatter. Following the OS regional format was weighed and declined:
      the rail would then disagree with Zotero's own counts beside it
- [ ] D7 the hop rail's numbers do not add up to a story the reader can
      follow. Raised by the user 2026-09-13, mid-walk, looking at a live
      fill: "What do the numbers mean then? If it keeps growing why do we
      have multiple places 'predicting' the size and one predicting the items
      left? This doesn't make any sense." What is on screen is
      `Hop 3 1,557/1,557 of 2,726` over `expanding · 627 left · Stop`, and
      the three numbers are in two units: 1,557 and 2,726 count papers, 627
      counts parents still queued to expand. Of the three, `shown/available`
      is a tautology while the fill runs (what is reached is what is drawn),
      `of {reported}` climbs as each expanded parent adds what it claims
      exists, so it looks like a target that runs away, and only the parent
      count converges. Nothing labels which is which. The spec's reasons are
      sound one at a time (the reported count exists so `50/50 of 1,200`
      never reads as complete; the percentage bar and the `≈ {estimate}` were
      dropped because a lazy fill cannot estimate a whole hop before its
      parents are expanded) — the failure is that together they read as three
      competing predictions. This is a design question, not a defect: the
      code does what `2026-09-12-citation-hops-design.md` specifies. Wants a
      brainstorm on what a reader actually needs during a fill — probably one
      number that converges plus a plain statement of what is still unknown.
      The Stage 3 progress-line check is left open against it.
      The user proposed an answer on 2026-09-13, after watching a fast fill
      appear, count down and disappear cleanly: "We could have a progress
      bar too, would look nicer." Worth weighing against the spec, which
      dropped exactly that — the percentage bar went because a lazy fill
      cannot know a hop's size before its parents are expanded, so a bar
      would have to be drawn against `of {reported}`, the one number that
      runs away. A bar over the parent count, which is the only figure that
      converges, is the version the spec's reasoning does not rule out; that
      is also the number with no label today
- [ ] B50 the hop fill slows to about one expansion every 15 seconds. Seen
      2026-09-13 during the manual walk, on a hop-3 fill that had already
      expanded 173 of 800 parents: "It is running really slow now. Expanding
      one item every 15 seconds or so." At that rate the 627 parents still
      queued are over two and a half hours of wall clock for one hop, which
      is why the Fetch more state could not be reached and why the Stage 3
      progress-line check above is part-walked. Not yet measured: whether
      this is the provider rate-limiting a session that has already made
      hundreds of requests, a backoff the runner applies after a 429, or the
      runner's own pacing; whether it started slow or degraded; and whether
      the request in flight or the rebuild between landings is the cost.
      Relevant that the counts kept climbing throughout, so the work was
      landing, just slowly. Chase it with a measurement of the fill's request
      timings before changing anything.
      Measured 2026-09-14, in the test Zotero rather than the user's session,
      by wrapping `Zotero.HTTP.request` in the Citation hops suite. The fill's
      15 seconds is not the network and not the rebuild: every failed
      expansion landed at 15.0–15.2 s, which is `RELATIONSHIP_PROVIDER_TIMEOUT_MS`
      in `externalDiscoveryService.ts`. `withProviderTimeout` races the
      provider lookup against it and resolves `null`, so the expansion is
      failed, but nothing cancels the lookup, which keeps retrying in
      `http.ts`. What kept the lookup past 15 s was Semantic Scholar answering
      HTTP 429 in about 0.46 s per request (23 of 25 through hop 1 in one
      run), and two things in the plugin that turn one refusal into many: the
      retry backoff (1, 2, 4 s on top of the 1.1 s spacing), and
      `lookupProviderRecord` falling through to `searchExactTitle` on any
      non-success, a 429 included, so each refused DOI lookup adds refused
      title searches. Neither profile has a Semantic Scholar key (the user's
      has an OpenAlex key only), so all of it is the keyless pool. A shell
      polling the same path at the plugin's pace, in the same seconds, got
      200 while Zotero got 429 (23:09:36, 23:13:07–21), then itself got 429
      continuously from 23:14:43 after about 190 requests: the provider does
      limit this IP over some window, but Zotero's requests were refused
      first. The probe dropped query strings, so whether Zotero's `fields`
      or Firefox's headers are what differs is not known. Still unmeasured:
      the user's real session, where the counts did climb, so its 429 rate
      and whether every slow expansion there was this. Not fixed on purpose,
      on the user's call; the handling (no title fallback on a 429, cancelling
      on the timeout, a fill that backs off) wants a brainstorm with D8
- [ ] D8 the fill fetches far more than the reader needs, and the cost lands
      on the whole plugin. Raised by the user 2026-09-13, mid-walk, after
      watching a hop-3 fill crawl: "This raises a real issue with the user
      experience of this tool. We need more ways to minimize the number of
      fetchs. The whole plugin also get's slower when the fetch is happening
      in large numbers. Hop 3 so far seems to be a hard limit, it would take
      another 2 hours just to start Hop 4." Three claims, and the third is
      the one that bites: a depth the rail offers to 6 is in practice capped
      at 3 by wall clock, so the deep-hop behaviour the design is built
      around is unreachable on a real library. The second claim was narrowed
      by walking the item-tree check against it the same day: Zotero's own
      panes stay responsive, "only the plot lags", so the cost is the
      graph's own rebuild and redraw per landing and the column coalescing
      is doing its job. Distinct from B50, which is the raw rate; this
      is whether a breadth-first walk of every parent is the right shape at
      all. Things a brainstorm should weigh: expanding only what the reader
      has looked at or asked for, expanding the most-cited parents first and
      stopping, a budget the reader sets, reusing what the store already
      holds before any request, and whether hops past 3 should be offered at
      all until this is answered. Stage 4 builds on the fill, so this wants
      settling before Stage 4's spec, not after. A second reason the deep
      hops are not worth their cost, from the same walk: in the citers
      direction the chain runs out against the present. "I can go as far as
      Hop 6 by skipping the other Hops, but there are really no Hop 6 items.
      Hop 5 is already 2026, so in this case there are no new items yet."
      Each hop of citers is roughly a step forward in time, so a seed of any
      age reaches the current year within a few hops and the rows past it are
      structurally empty, not merely unfetched. That is a reason to derive
      the depth the rail offers from the data rather than fix it at 6, and it
      also means a row reading `not fetched` and a row that can never have
      anything are indistinguishable today
- [ ] D9 under the Citation hop colouring the rail shows the hop colours
      twice. Noticed by the user 2026-09-13 while walking the Stage 3
      colouring check: "When I slect 'Citation Hop' for the nodes, it now
      shows the colors in two places in the rail. One under the Citation
      Hops section and again in Color." Both halves are specified and each
      is reasonable alone — the hop rows carry the hop's category colour
      while the binding is Citation hop and a neutral dot otherwise, so the
      rail never suggests a colour the plot is not using, and the Key's
      category section lists the categories of whatever colouring is bound.
      Together they are the same ledger twice, in one rail, a few rows
      apart. The question is which one should yield: the Key section is the
      general mechanism and the hop swatches are the one the reader is
      already looking at. Design, not a defect
- [ ] B51 opening a deeper hop strands the shallower one: its remaining work
      can no longer be stopped or resumed. Found 2026-09-13 during the manual
      walk, on a hop-3 fill left part-expanded (627 of 800 parents still
      queued) when the user opened hop 4: "the interface still let me
      partially skip the Hop 3 and start fetching Hop 4 (which is nice since
      I can fetch directly from a specific node). The downside is that we
      cant fetch from Hop 3 anymore since the Resume button now shows only at
      Hop 4." The rail follows the spec — the progress line sits under the
      deepest open hop — but the spec never asked what happens to a hop left
      unfinished beneath it. Two things to establish: whether hop 3's queued
      parents are still in the plan and merely unreachable from the rail, or
      dropped; and whether the reader should be able to hold two hops'
      progress at once. Note the user reports the skip itself as a feature,
      so the fix is not to forbid it
- [ ] B52 the Key keeps the hop colours after the colouring changes away from
      Citation hop. Found 2026-09-13 during the manual walk: "when the color
      settings for the nodes is set to citation, the Color section in the
      rail still shows the colors of the Hops as if selecting The settings as
      Citation Hop." So the rail is stating a colouring the plot is not
      using, which is the exact thing the neutral-dot rule for the hop
      swatches exists to prevent. Distinct from D9, which is the same colours
      shown twice while Citation hop _is_ bound
- [ ] D10 hop depth is encoded as opacity, and opacity is the wrong channel
      for it. Raised by the user 2026-09-13 walking the Stage 3 emphasis
      check: "I can see that there is a opacity ramp, but it wasn't clear
      until Hop 5. There should be another way to see the diference in Hops."
      The ramp is `1, .9, .8, .7, .6, .5, .4`, so consecutive early hops
      differ by a tenth and are not tellable apart; the difference only
      becomes legible once several steps have accumulated, by which point
      the node is also nearly gone. The same report carries the other half:
      "the opacity still works when set to Citation Hop, which doesn't make
      sense (now we have both the Citation Hop color and the opacity
      happening in the view." The spec applies the ramp "under every
      colouring" deliberately, so hop stays readable whatever the gear is
      bound to — but under Citation hop that double-encodes one variable in
      two channels, and spends opacity, which is also what emphasis and
      search dimming use. Worth weighing: a channel that separates adjacent
      hops (ring, stroke weight, size), keeping opacity for emphasis alone,
      and dropping the ramp when the colouring already says hop
- [ ] D11 a reference fill lands a lot of papers with no readable metadata.
      Raised by the user 2026-09-13 walking the Cornerstones check: "The
      references have a lot of Unknowns. We should investigate a way to
      resolve these unknowns." References are the direction where a provider
      most often returns an identifier and little else, so the hop lands a
      paper the reader cannot judge — no author, no year, which also parks it
      in No data (see B48) and makes it worthless on both axes. To
      investigate before designing: how many of the Unknowns carry a DOI or
      other identifier a second lookup would resolve, whether the
      membership-first fetch drops metadata the provider did return, and
      whether unresolved papers should be drawn at all or held until they
      resolve. Nothing is measured yet. Mechanism, from the spec, worked out
      with the user on 2026-09-13 when they asked "How do we know we have
      unknowns? And if we know we have unkowns, we should be able to at least
      know the author and title, or am I missing something here?": an
      expansion runs "membership first, summaries hydrated cooperatively", so
      the edge list (identifiers) lands first and the title, author and year
      arrive later in idle slices. An Unknown is therefore a paper we know
      exists by identifier whose summary has not hydrated or whose hydration
      failed — and the user is right that the identifier is what a second
      lookup would resolve. First hypothesis to test: with the fill crawling
      at one expansion every fifteen seconds (B50), the cooperative
      hydration is starved behind the membership requests, so the Unknowns
      are a symptom of the rate and not of missing data. Worked examples the
      user pulled on 2026-09-13, which point at a second cause: the paper
      `10.1145/1201775.882296` has no year of its own, and two of its
      references, `10.5555/839277.840020` and `10.5555/826029.826529`, come
      back "DOI Not Found". Both failures share the `10.5555` prefix, which
      is what legacy ACM and DBLP proceedings records carry and is frequently
      not registered with the resolver — so this class of Unknown is not a
      paper we failed to fetch but a reference whose DOI was never
      resolvable. Check that before anything else: if it holds, the fix is a
      fallback (title and author lookup, or the provider's own identifier
      rather than the DOI) and not more fetching. The parent having no year
      is B48's territory
- [ ] B53 the File menu runs Open's saved-graph rows straight into Save and
      Save as… with no separator. Reported 2026-09-13 during the manual
      walk: "In the File the 'Save' and 'Save as...' are listed under the
      'Open' section. Add a horizontal divider there." Small, and in the same
      menu B37 and B16 just settled
- [ ] B54 verify that reopening a filled graph does not refetch what the
      store already holds. Prompted 2026-09-13 by the user's recollection
      mid-walk: "I remember that in a brainstorm we decided to not save the
      hops, but to let it regenerate for every load. I think that this is not
      a good idea since fetching is a bottle neck here." The spec says the
      expensive half is safe — expansion results "land in the relationship
      store, survive a direction switch and a reopen" — and only the derived
      bookkeeping (which papers are expanded, the plan, the caps, the
      reported counts) is recomputed, at no network cost. So there may be
      nothing here. What is worth checking against B50's rate, since nothing
      has: that a reopen of a graph filled to hop 3 draws its papers without
      a fresh round of expansions, and that the per-hop caps resetting each
      session does not restart the fill from the beginning. One known leak
      either way: failed papers "leave the plan for the session and come back
      after a reopen", so every reopen retries them
- [x] B56 the tutorial card does not survive a plot narrower than 400px,
      which is the width the D4 check names. Found 2026-09-13 walking that
      check on the 43dfe4b XPI: down to 400px the card is fine; "under that
      the container is still trying to fit the content even though the width
      got too small. This streches the hight a lot and the text becomes
      unreadable and everything looks like it has an ugly bug." So the card
      has no floor — it keeps honouring its content's intrinsic width by
      growing downward instead of reflowing, clamping, or standing down. The
      check asks for the card to still fit _under_ 400px, so this is the
      clause that fails. Unrelated to B44: this card's layout is otherwise
      sound, it is the narrow case that is unhandled.
      Fixed 2026-09-14, direction A of the three the user was shown
      (compact, stand down, pill). Measured first in Zotero's main window
      with both stylesheets loaded: the card holds 300px down to a 328px
      plot (its width plus the 14px insets), then narrows as
      `calc(100% - 28px)` with no floor — 237px tall at 328, 254 at 280, 325
      at 240, 364 at 200, where the text stands in a 142px column with one
      chip per line. Nothing overflowed and nothing was clipped (the rail
      never stacks, so the plot keeps its height); the card was simply
      reflowing into a strip. The spec never said what a narrow plot gets.
      Below a 328px plot the card now keeps only the view's name, × and
      "Don't show for this view again", about 80px tall; the paragraph,
      chips, footnote and "Got it" (which × duplicates) return when the plot
      widens. It is a container query, and the container is a new
      click-through layer holding the card (`.cm-view-card-layer`), not
      `.cm-graph-area`: `container-type` brings layout containment, which
      would make the plot a stacking context and flatten the node menu and
      popovers that share it. Guarded by a case in
      `test/zotero/graphViews.test.ts` that narrows the real tab's plot to
      240px (red: card 212x370.5 with everything shown) and back to 400px
- [x] B57 the save panel's inline name warning is too small to read. Found
      2026-09-13 walking the D4 save-view check, where the refusal itself
      worked: a name matching a shipped view is refused inline as specified,
      but "the warning text is too small". It is the one line standing
      between the reader and a rejected save, so it should be the most
      legible thing in the panel rather than the least.
      Fixed 2026-09-14, option A of two the user was shown: the panel's own
      size and a readable ink in dark. Measured first: 11px in a 12px panel,
      and the colour was `var(--cm-state-retracted, #a44c00)` — a token
      nothing defines, so the fallback in both schemes, which is fine on the
      light panel and 2.94:1 on the dark one's `#1c1b22`. Size was half of
      it; in dark the line was also below readable contrast. It is now 12px
      in a new `--cm-warning-ink`, `#a44c00` in light and `#e8894a` in dark.
      Guarded in `test/zotero/hostButtonHeight.test.ts`: the size against
      the panel's, and a 4.5:1 floor on both schemes
- [ ] B55 a graph saved while its fill is stopped reopens fetching. Found
      2026-09-13 during the manual walk: "when I saved I had the fetch state
      on Stop, and when I loaded it had the fetch state on Resume. This
      should be preserved. Now it started fetching without me asking for
      this." The spec put the paused flag among the things deliberately not
      persisted, so the code is doing what was written — but what was written
      costs the reader a fill they did not ask for, on every reopen, at
      B50's rate. Persisting the flag is one line of state; the question the
      fix should answer is whether a reopen should ever start fetching by
      itself, given hop 1 is also specified to open the moment a graph gains
      a seed
- [x] B58 a folder renamed in Zotero keeps its old name in the graph until
      Zotero restarts. Found 2026-09-13, volunteered by the user while
      walking B31's narrow-rail check: "I updated a folder name in Zotero,
      but the graph did not pick the change. Even after clicking Refresh at
      the top. Even new graphs don't pick the renames. It only works once I
      close and open Zotero." The suspect is named and the three symptoms
      all follow from it. Collection names are read live —
      `zoteroLibraryService.ts:196-235` builds `allCollectionInfo` straight
      off `Zotero.Collections` — but that runs inside the whole-library
      snapshot, which is a process-wide cache
      (`zoteroLibraryService.ts:32-57`) invalidated from exactly one place:
      the notifier in `hooks.ts:206-210`, registered for `["item"]` alone.
      A rename fires a `collection` notification, which nothing observes, so
      the snapshot keeps the old name; new graphs read the same cache, which
      is why a fresh graph shows it too; Refresh never calls
      `invalidateWholeLibrarySnapshot`, which is why it does not help; and a
      restart empties the cache, which is why only that works. The narrow
      fix is to add `"collection"` to that observer's types (and to
      `automaticUpdateCoordinator.ts:166`, which is `["item"]`-only for the
      same reason). Worth asking while fixing whether a rename should
      invalidate the whole snapshot at all, or just repaint the rail's
      labels — a rename changes no paper and no citation, so dropping the
      library snapshot to relabel one row is a heavy answer
      Fixed 2026-09-14, relabel in place, as the user chose from four options
      (spec `2026-09-14-folder-changes-reach-graph-design.md`, plan of the
      same name). The entry's narrow fix was wrong in both halves: dropping
      the snapshot leaves an open tab stale, since a view builds its folder
      lookups once at mount, and `automaticUpdateCoordinator.ts` queues
      citation fetches, so adding `"collection"` there would have started
      network work on a rename; it is untouched. Now the snapshot observer in
      `hooks.ts` also takes `collection` notifications and rebuilds only the
      cached snapshots' folder lists (`refreshSnapshotFolders`, in the new
      `libraryFolders.ts`), then publishes a bare event
      (`libraryFolderEvents.ts`). Each open graph refreshes its own snapshot,
      refills its label and descendant maps in place, drops a gone folder's
      region and redraws the rail. It never remounts, because a remount is a
      reopen and by B55 a reopen restarts a stopped fill. A trashed folder is
      skipped on both of the builder's paths. Folded in at review instead of
      filing B62: a graph opened from folders records which folders its title
      came from, and a title still at its default follows a rename; a typed,
      saved or restored title is never touched. What Zotero answered, from
      the suite's run: a rename fires `collection:modify` alone, carrying the
      old name in `extraData`; a delete fires `collection:delete` with
      `libraryID` and `key` and no `item` notifications; a trash fires
      `collection:modify` then `collection:trash`, and `getByLibrary` no
      longer lists the trashed folder. Guarded by
      `test/zotero/folderChangesReachGraph.test.ts`, which opens the folder
      graph from Zotero's own collection menu after
      `ZoteroPane.buildCollectionContextMenu()`; that call is what adds
      plugin entries, and a bare `openPopup` shows Zotero's entries alone
- [ ] D12 there is no way to tell how complete a filled plot is. Raised by
      the user 2026-09-13 during the manual walk: "it would be good to know
      whether we skipped some papers, right now there is no way I can tell
      how healthy is the plot I have." Three kinds of gap are invisible
      today and none is distinguishable from a paper that simply has no
      citers: papers whose expansion failed (the failed set is per session
      and not persisted), papers never reached because a cap or a Stop cut
      the plan short, and papers drawn without metadata (D11's Unknowns).
      The rail's counts do not separate them — D7 is about the same rail
      saying too little with too many numbers. What a reader needs is
      probably one plain statement of what is missing and why, not another
      count
- [ ] D13 at a filled graph's density the edges stop meaning anything.
      Raised by the user 2026-09-13 walking the hop-3+ legibility check on a
      plot of roughly 1,500 papers: "I can read them, but at this level the
      arrows are just a mess. Not showing any information really." The nodes
      survive the crowd because each one is still a paper in a place; the
      edges do not, because a line between two points only reads while you
      can follow it, and at this count they are a wash. Worth weighing: not
      drawing edges past a density threshold at all, drawing them only for
      what is hovered, selected or seeded, bundling them, or replacing the
      whole-graph edge layer with something aggregate. Note the Key currently
      spends three rows on Link, Reference and Cited by, so the rail is
      explaining a channel the reader cannot use. Related but not the same as
      D10, which is about how hop depth is encoded
- [ ] D14 the gallery's inset leaves a sliver of plot showing that says
      nothing. Raised by the user 2026-09-13 while walking the D4 gallery
      check, which passed: "the gallery has a slightly smaller size than the
      plot, so I can still see 'CITATIONS' to the left and the number 500 at
      the top. If we have a gallery this big, why not just have it filling
      the full plot size already?" The inset is deliberate — it is what makes
      the gallery read as non-modal, with the rail and toolbar live behind
      it — but the margin is too small to carry that meaning and what shows
      through is an axis label and a tick, which describe a plot the reader
      has not chosen yet. Either commit to the full plot area, or make the
      margin wide enough to read as a card floating over a graph. Design, and
      small
- [ ] D15 transient confirmations are too quiet to notice. Raised by the user
      2026-09-13 walking the save-view check, where Copy JSON did show
      "Copied" in the toolbar as specified: "the feedback is almost
      invisible. I would suggest to use a sonner at the bottom right corner
      (just like what shadcn does)." The toolbar status is doing double duty
      today — it carries the read-only migration notice, which must stay up,
      and fleeting confirmations like "Copied" and "Saved", which must not —
      and one slot cannot make both read well. A toast in a corner would
      separate them: the toolbar keeps what persists, the toast takes what
      passes. Worth deciding once, since B42's and B55's notices, the
      autosave and every future confirmation land in the same place
- [ ] D16 the gallery offers only the shipped views, never the reader's own.
      Noticed by the user 2026-09-13 walking the D4 icon check: "I don't see
      a card for user saved views in the gallery." The spec is the reason —
      it lays the gallery out as three columns of the five shipped views plus
      a last card holding Start blank and Import view JSON…, and saved views
      were never part of it; they live in the View dropdown alone. But the
      gallery is the one moment the question is actually asked, "How do you
      want to look at these N papers?", which is exactly when a view the
      reader built for their own work is worth most. It also means the
      `view-user` icon has no 28px rendering anywhere, so that half of the
      icon check cannot be walked. Small design decision: whether saved views
      get cards, a row beneath the columns, or a line on the last card
- [ ] D17 a graph's appearance should not be inherited from whatever the
      reader last did. Raised by the user 2026-09-13 walking B41, the item
      that deliberately built the inheritance: changing the y axis on one
      seeded graph and closing it makes the next seeded graph open the same
      way. "I don't like that, it breaks the user mental model of 'I know
      what a default graph look like', making the user lost." The mechanism
      works as B41 specified — `getFocusGraphAppearance` falls back to
      `xMetric: "citation-sequence"` and a stored `focusGraphAppearance`
      overrides it — so this is a disagreement with the design, not a defect.
      The user's own comparison is the argument: they liked that
      Tools › Meristema › New Graph "stays like that independent on how the
      user edited it before", so a stable default is what they expect of a
      new graph and the seeded path is the odd one out. What to weigh: a
      fixed default with per-graph edits that do not escape the tab, against
      a remembered default the reader can reset; and whether a graph that has
      drifted should say so, the way the view chip already says "(edited)".
      One thing the inheritance does _not_ break, established walking B41's
      reopen check on 2026-09-13: a graph saved with a view applied reopens
      on that view with a clean chip, so the stored appearance does not
      override what the graph itself saved
- [x] B61 the Key rail's entries wear the base button rule's box, not their
      own. Found 2026-09-13 by the unprefixed-rule audit B59 asked for, not
      by a report. `.cm-key-entry` is a `<button>` (`graphKeyRail.ts`) styled
      by a bare `.cm-key-entry` at (0,1,0), which loses to the shared
      `.meristema-root button` at (0,1,1) in `paperDetail.css` for every
      property both declare. Measured in the main window: the entry computes
      `min-height: 28px`, `padding: 4px 9px`, `gap: 5px` and
      `border-radius: 6px`, where its own rule asks for no minimum, `3px 4px`,
      `7px` and `4px`. So every Key row is taller and more indented than the
      rail was drawn for, and the `:hover` pair has the same problem one notch
      up — `.cm-key-entry:not(.cm-key-entry-static):hover` is (0,2,0) against
      the base's (0,3,1), so a row's neutral 8% tint comes out accent blue,
      which is the same symptom the user reported on the Scope row in B59.
      Someone has already been here: `.meristema-root button.cm-key-entry`
      (`graph.css:1303`) exists and neutralises the border and the background,
      so only the box is still wrong. The fix is the pattern B59 now uses —
      fold the bare rule into that prefixed one. Cosmetic, and nobody has
      complained, so it is worth deciding whether the rail is actually wrong
      to the eye before moving it: the 28px floor may be what makes the rows
      comfortable to click. Check against the rail's design in
      `2026-09-11-scope-rail-folders-design.md` before touching it.
      Fixed 2026-09-14, the hover only by the user's call: the 28px rows stay
      as they are. Measuring the hover found a second loss the entry above
      did not name. The prefixed reset's `background-color: transparent` at
      (0,2,1) also outranked `.cm-key-entry[aria-pressed="true"]` at (0,2,0),
      so a pinned entry painted nothing at all, where the rail's design gives
      it an 18% accent tint; the user asked for that fixed in the same change.
      Both rules now carry the `.meristema-root button` prefix, (0,4,1) and
      (0,3,1). Two cases in `hostButtonHeight.test.ts`, each compared with a
      swatch of the colour its rule names: red (the hover painted the base
      rule's accent surface, the pinned entry `rgba(0, 0, 0, 0)`), then green,
      that file 11/11 run alone
- [x] B62 a saved version 4 graph loses its "Directions are now one at a
      time; showing Citers" notice for good on any refresh of the open graphs.
      Found 2026-09-14 as the one failure of an 84/1 suite: the v4 case saw
      the notice go straight to "". Not a timer — `setStatus` clears the old
      one before writing. `refreshOpenGraphViews` re-renders every open tab
      through `renderTab`, whose `captureViewState` replaces the instance's
      state with the live view's `getState()`, and that never carries
      `migratedFromBothDirections` (only a parse sets it), so the remounted
      view has nothing to announce. The read-only notice survives the same
      remount because `renderTab` calls `announceReadOnly` after every render.
      What triggers a remount: any item trashed or deleted (the 250 ms removal
      refresh in `hooks.ts`) and a citation update with `refreshGraph` — in
      the suite, the fixture's own update landing just after the v4 tab
      opened, which is why the case passed whenever Semantic Scholar was
      refusing. Proven with a probe: erasing a scratch item under the notice
      replaced the tab's root and status bar about 400 ms later and the bar
      stayed empty. The spec ("State") promises the notice until the state
      is saved again as version 5, which a remount is not.
      Fixed 2026-09-14, on the user's call to fix rather than file. The flag
      now lives on the graph instance beside `savedGraphReadOnly`: set when a
      saved graph opens from a version 4 `both` record, cleared by Save as,
      and announced after every tab render by `announceMigratedDirections`,
      the way `announceReadOnly` is. A first attempt carried the flag through
      `captureViewState` and stayed red: `onStateChange` replaces the
      instance's state with the view's on every change, so the flag was gone
      long before the remount. The v4 case gained a remount step (erase a
      scratch item, wait for a new root, expect the notice back): red, then
      green in a full suite of 85/0. Left open that day and closed 2026-09-15
      on the user's call to test first: `renderDetachedWindow` re-renders on
      a refresh and called `announceReadOnly` but not the new announcement.
      A new case 8 moves the version 4 tab through Zotero's own tab menu
      (Move › Move to New Window, which calls the plugin's `moveToNewWindow`
      hook) and forces the same remount on the window. Red, and earlier than
      expected: the detached toolbar read "" straight after the move, before
      any remount, since the move renders the window from the captured view
      state too. The same one call in `renderDetachedWindow` turned it green;
      it is the suite's first case to open a detached window

## Architecture pass (2026-09-13, branch `architecture-pass`)

Run with `/improve-codebase-architecture` after Stage 3 merged, with
`CONTEXT.md` and ADRs 0001 to 0012 as settled inputs and the two Stage 3
reviews' Minor lists as the backlog. The report (six candidates, before and
after diagrams) is at `%TEMP%rchitecture-review-2026-09-13.html`; the
ledger's "architecture pass" section has the record. Every change gated by
`npm run check` and a captured `npm test`.

- [x] M14 first: the seeded graph's citation sequence reaches the plot by
      map through one renderer metric reader (ADR 0008's shape)
- [x] Minors: M3 vocabulary, M8, M11, N4, the duplicated unhide, the linear
      finds, readonly plan arrays. M7 judged not a defect (bare `.cm-` is the
      file's norm). Left as recorded: M6 and M13 (behaviour, need a
      brainstorm), M12 (a service-path coverage gap), N2 (suite dependency)
- [x] Candidate 2, seed marks: one record replaces five renderer setters
- [x] Candidate 1, the fill: `graphHopFillRunner.ts` owns the runner's state;
      eight unit cases drive it with a fake expander
- [x] Candidate 6: three renderer methods nothing called, gone
- [ ] Candidates 3 to 5 (state, then plot, then chrome; the rail's inputs from
      their owners; the focus vocabulary rename) wait for a decision; 3 is
      large and comes after Stage 4's spec, not before
- [x] merged to main fast-forward at 2ba2696, pushed 2026-09-13; XPI built
      from the same tree

## Manual verification (the user checks these in Zotero, in one batch)

Each session that merges an item appends its checks here instead of
asking the user to verify right away. Install the XPI from
`.scaffold/build/meristema.xpi` (built from the latest main), then walk
the list; tick what passes, and turn any failure into a new backlog
entry.

- [x] B1: Graph › Save as… shows a dialog titled "Meristema", no
      question-mark icon, tab title prefilled.
- [x] B1: × on a row in the Open list shows a "Meristema" dialog with a
      Delete button and Cancel, the explanation on a second line; Cancel
      keeps the graph, Delete removes the row.
      Both B1 checks passed as written, and walking them found two new
      defects in the same dialogs: backlog B8.
- [x] B2: after a restart, Tools › Meristema › Open Saved Graph lists the
      graphs the first time it opens; "No saved graphs yet." shows only
      when there are none. (Also covered by `test/zotero/savedGraphMenu.test.ts`.)
- [x] B3: Tools › Meristema › New Graph, then right-click the tab › Rename
      View…, give a name: the Open list (Graph menu and Tools) shows a row
      with that name, and the dialog is titled "Meristema". (Also covered by
      `test/zotero/savedGraphRename.test.ts`.)
- [x] B3: rename the tab of a saved graph: the row shows the new name.
- [x] F2: mouse wheel over a graph: about sixteen notches take the view
      from the smallest zoom to the largest (roughly half what it took).
- [x] F2: trackpad pinch or two-finger scroll over a graph: the zoom is
      smooth and continuous, not jumpy; a fast fling does not throw the
      view across the whole range in one step.
- [x] F1: right-click a node whose item has a PDF: the menu leads with
      "Open PDF"; choosing it opens the reader tab in the main window
      (from a detached graph window too, which comes forward).
- [x] F1: right-click a node with no attachment but a DOI or URL: the
      entry reads "Open online" and opens the browser; a node with
      neither shows no open entry. An external node opens its provider
      page. Arrow keys cycle through all entries.
- [x] B6: New Graph, add a seed, add a paper that is not in Zotero as a
      seed, Add to Zotero from its pane: the item appears in the library
      and, within a second or two, the node turns local (the pane shows
      the library paper, the Key counts it as local); both seeds stay.
      (Also covered by `test/zotero/externalSeedImport.test.ts`.)
- [x] B6: the same with a paper that has a DOI: the import goes through
      the DOI translator and the seed turns local as well.
      Both B6 checks passed as written on 2026-09-08, which closes the
      batch: every Stage 1 item is now verified in Zotero.
- [x] Stage 2: a folder graph plus a seed keeps the folder's other papers on
      screen. (Also covered by `test/zotero/graphScopeRail.test.ts`.)
- [x] Stage 2: unticking every folder on a seeded graph leaves the seeds and
      their neighbours; ticking them back restores the rest.
- [x] Stage 2: a seed's citer already in the library wears a thin ring; one
      that is not does not. Seeds keep their bullseye, in their own colour.
      The marks are as written, but the colours behind them are not: folder
      colours are dealt by rank so a tick repaints the others (B12), and
      seeds, the ramp and the in-library ring all draw on the same greens
      (D3, which is the one to settle first).
- [x] Stage 2: hovering a Seeds row lights that seed and its edges, and moving
      to a Key entry strands no highlight.
- [x] Stage 2: the seed search panel opens from the rail's "+ Add seed", keeps
      its width over the plot, and its rows have clear space between title and
      metadata. Two defects on the way in: the link is too dark to read on the
      dark theme (B13) and a row takes two clicks to seed (B15).
- [x] Stage 2: File and Tools › Meristema offer New Graph, Open, Save and
      Save as… in that order; in Tools the last two are greyed with no graph
      tab open. The menus themselves are right; their dressing is not — the
      Open list centres its names (B16), Tools repeats the plugin icon on
      every row (B17), File sits fourth in the plot toolbar rather than
      first (B21), and New Graph from a detached window looks like nothing
      happened (B20).
- [x] Stage 2: right-clicking papers offers "New Graph from N items" and an
      "Add to" submenu listing the open graphs, and no Refresh; Refresh is in
      Tools › Meristema.
- [x] Stage 2: a graph saved before this release opens showing the same papers
      it drew before — in particular one scoped to a parent folder still draws
      that folder's whole subtree. The migration holds; no saved work was
      lost.
- [x] Stage 2: open a saved graph whose seed is a paper outside Zotero and add
      it to Zotero: within a second or two the node turns local, and reopening
      the graph shows the library paper rather than the external copy. (The
      recipe used to keep the external seed when nothing repainted; also
      covered by `test/zotero/externalSeedImport.test.ts`.) A graph left with
      only an external seed also saves and reloads correctly — what is missing
      is a way to _start_ one from a paper outside Zotero, filed as F6.
- [x] B28: after installing the XPI built from this fix, a graph opened from a
      folder draws its nodes, and selecting a folder's row on any graph draws
      the region without losing the plot. The Error Console stays quiet.
      (Walk this one first: the six D3 checks below all depend on it.)
      Confirmed by the user on 2026-09-10: it works. The regions read as
      polylines, though — filed as D6.
- [x] D3: a folder graph opens with its own folder drawn as a shaded region
      behind the nodes, in that folder's colour.
      Failed on 2026-09-10 (B28, the plot blanked). Passes on 2026-09-11, on
      the b2ff41d build.
- [x] D3: clicking a second folder's row draws a second region; clicking it
      again removes it. Selecting a fifth folder drops the first.
      Passes as written on 2026-09-11. The user wants the cap gone, though:
      "it may be frustrating for the user" — F14.
- [x] D3: ticking and unticking folders never changes any other folder's
      colour (B12), and unticking a selected folder clears its region.
      Passes on 2026-09-11. One deep purple swatch does not read against the
      dark background: B32.
- [x] D3: with four seeds and colour by citations, every seed is plainly not
      a ramp colour, and no seed, ring or region shares a green.
      Passes on 2026-09-11.
- [x] D3: removing the first seed leaves the other seeds' colours alone.
- [x] D3: a graph saved before this release opens showing the folders it was
      made from as regions; a whole-library graph opens with none.
      Passes on 2026-09-11: the version 2 migration holds and no saved work
      was lost. Blocked by B28 on 2026-09-10. The graph that opened and drew its papers was a
      non-folder one; a pre-release graph made from a folder blanks like
      every other region path. Nothing says saved work was lost — the
      migration is simply unreadable until B28 is fixed. Rewalk then, on a
      graph still on state version 2, and do not save it in the meantime.
- [x] D3: zooming in does not split a folder's region into pieces, and the
      region keeps a constant clearance around the node discs.
      Passes on 2026-09-11, read against D6: the territory pulls apart into
      its papers past the fit zoom by design, and the clearance holds.
- [x] D3: a selected folder's row carries a coloured border beside its name —
      the rail's region legend — and it reads clearly against both themes.
      The legend reads in both themes as written. Switching light → dark
      leaves the plot's own background light, though: B29.
- [x] B11: with a graph open, click exactly one row in the library list: that
      paper's node is selected in the graph and the detail pane shows the
      paper rather than "Select a paper to inspect…". Then select three rows:
      the pane goes back to that empty state and the present nodes emphasise.
      (No product code changed for B11 — this walks the behaviour the suite
      now asserts, which had never been checked by hand.)
      Passes: one row rings its node, two and three rows leave the selection
      opaque and the rest transparent. Three findings on the way: a node
      whose paper is outside the folder Zotero has open selects some other
      item instead of clearing (B30), the graph cannot drive a selection back
      into Zotero and cannot multi-select at all (F10), and the one-row ring
      and the multi-row opacity are two visual languages for one idea (F10).
- [x] D6/shapes: a folder graph at the fit zoom. The territory reads as a
      smooth blob and its outline is a curve with no wobble and no facets.
      Passes on 2026-09-11: "it is getting good". One thing on the way: two
      halos deform toward each other before they merge and meet in a point
      rather than a metaball's neck — the kernel, not the fit: B33.
- [x] D6/shapes: zoom in until the papers separate. A lone paper's halo is a
      clean circle at every zoom — it is drawn as an exact arc now, not fitted
      through a grid — and the territory pulls apart into those circles
      smoothly.
      Passes on 2026-09-11.
- [x] D6/shapes: the offset at `FALLOFF_FRACTION = 0.03` is right, or says
      which way to move. Reasoned, not measured: drawing lone papers as exact
      arcs hands back about half a pixel the fit's inward bias was removing,
      so it is not simply a quarter tighter than 0.04. It is one character to
      change and the grid no longer gets a vote on it.
      Walked 2026-09-11: "make it 0.005 smaller". Now 0.025
      (`falloff-0025`); the next build is the check.
- [x] D6/shapes: the offset at `FALLOFF_FRACTION = 0.025` reads right at the
      fit zoom and zoomed in, or says which way to move again. Passed
      2026-09-11 on the 8ee1467 build: "Looks good."
- [x] D6: the zoom-bucket check, reworded on 2026-09-11 after the user asked
      what "jiggle it a percent either way" meant. Zoom in past the fit zoom
      until a halo has visibly shrunk at least once. Then, without panning,
      turn the wheel one notch in and one notch out, several times, and also
      try a slow trackpad pinch that hovers around one zoom level. What should
      happen: the region changes size in discrete steps of about 12%, and
      between steps it holds still. What would fail: the region flickering
      between two sizes on a single notch, or breathing while the wheel is
      still — that is a bucket boundary being crossed and recrossed, and the
      bucket function would want a deadband (the spec says why it was left
      out).
      Passes on 2026-09-11: "no jiggle. The step works nicely." No deadband
      needed.
- [x] D6: the Error Console stays quiet through all of the above. `draw()`
      latches `canvasError`, so one throw blanks the plot permanently; a quiet
      console is the check that the curve builder is total.
      Walked on 2026-09-11 and the plugin threw nothing: the console carried
      Zotero's own locale, reader and devtools noise, one harmless warning of
      ours (a visually-hidden `h1` with no font-size) and one unattributed
      "Uncaught (in promise) undefined" with no stack, filed as B36 because
      the intermittent savedGraphMenu failure printed "undefined" too.
- [ ] D6: pan around a folder of 300+ papers at maximum zoom and watch for
      frame drops.
      **Fails** on 2026-09-11: "it has a delay to it." Filed as B34, for
      later at the user's request; the suspect below stands. Nothing has measured the draw path yet — `drawRegions`
      rebuilds every folder's `Path2D` from scratch on every frame, including
      loops that sit entirely off-screen, and the zoom tightening is exactly
      what fragments a large folder into many small loops at high zoom.
      This is the regime the shapes design makes cheapest rather than most
      expensive, so a frame drop here means the draw path — `drawRegions`
      rebuilding every `Path2D` every frame — and not the contour.

- [x] B30: open a folder in Zotero and select a paper in it, then open a
      library graph and click a node whose paper is not in that folder. The
      list clears rather than staying on the paper it had; the graph keeps
      the node you clicked; a click on a node whose paper is in the folder
      still selects its row. **Failed** on the 8ee1467 build, 2026-09-11:
      "It still won't clear the folder selection if the paper isn't in that
      folder." Reopened as B30 in the backlog, fixed, and passes on
      2026-09-13 on the 43dfe4b XPI: "It works". Walked deliberately, not
      ticked as superseded — the "B30, again" row below uses a node the
      library does not hold, which is a different case from this one, an
      in-library paper outside the open folder. One finding on the way,
      filed as B60: the clear leaves the old row focused, so it keeps a
      dashed ring and the arrow keys still move from it.
- [x] B13: on the dark theme, the rail's "+ Add seed" (and "Show all") read at
      a glance — a light blue on the dark ground, not the accent blue. On the
      light theme they are unchanged.
- [x] B14: hover the × on a Seeds row. Nothing is painted behind it — no disc,
      no ellipse; the × itself turns from muted to full ink and a little
      heavier. Keyboard focus on it does the same. **Failed** on 8ee1467,
      2026-09-11: "Now the whole container background behind the × gets
      dark. So instead of an ellipse, now we have a rectangle." Reopened as
      B14 in the backlog and fixed. Ticked 2026-09-13 as superseded, not
      rewalked: the "B14, again" row below walks the identical scenario on
      both themes and passed on the 676d3d1 build.
- [x] B16: File › Open lists the saved graphs with their names flush left,
      the date at the right edge of the row, the same alignment as New Graph,
      Save and Save as… above and below. **Failed** on 8ee1467, 2026-09-11:
      "I can't see the list because now the File is to the left and the
      container opens behind the left sidebar. It needs to open to the
      right." Filed as B37, which unblocked it. Ticked 2026-09-13 as
      superseded, not rewalked: the "B37, with B16" row below walks this
      alignment once the popup opens where it can be read, and passed on the
      676d3d1 build.
- [x] B17: Tools › Meristema carries the plugin icon on the Meristema entry
      only. New Graph, Open, Save and Save as… inside it carry none, and the
      saved-graph rows under Open carry none either. The item and folder
      context menus keep their icons.
- [x] B21: the plot toolbar reads File, Filter, Similar, Export, Refresh, with
      the search box still at the far right; the bar is still one row when
      the window is narrow.
- [x] B19: right-click one folder: the entry reads "Create a new graph".
      Right-click two folders: it still reads "New Graph from 2 Folders".

- [x] B30, again: with a folder open in Zotero and one of its papers
      selected in the list, click a node the library does not hold (an
      online-fetched paper). The list clears; the graph keeps the node.
- [x] B14, again: hover the × on a Seeds row. No box of any shape appears
      behind it, on the dark theme and the light one; the × itself goes from
      muted to full ink and a little heavier.
- [x] B37, with B16: click File in the plot toolbar. The popup opens to the
      right of the button, fully visible over the plot; its saved-graph rows
      read flush left with the date at the right edge, aligned with New
      Graph, Save and Save as…. All three passed 2026-09-11 on the 676d3d1
      build; B16 passes with B37.
- [x] B35: right-click one paper in Zotero's list. The entry reads "New
      Graph with 1 seed" with no text at its right edge; with three papers
      selected it reads "New Graph with 3 seeds". Right-click a folder: "New
      Graph" and "New Graph from N Folders" no longer show "library only".
- [x] F14: tick five, then nine, folders in the rail. Every one keeps its
      region; none is released. The ninth to twelfth take new colours
      (lime, sky, coral on dark or maroon on light, plum); a thirteenth
      draws in the neutral Other grey. Untick any and the rest keep their
      colours.
- [x] B32: on the dark theme, seed four papers or more. The fourth seed
      disc is a mid purple that reads clearly against the plot; no seed
      disc looks like a hole.
- [x] B33: tick a folder whose papers sit at mixed distances. A lone paper's
      halo is the size it was before. Two halos approaching each other neck
      smoothly into one blob, with no point and no islet between them
      before they touch. Where three or more merge the outline is rounded
      throughout.
      All four passed on 2026-09-11 on the 59ebeac build. B33 with a
      remainder: "still has the gravity effect creating peninsula-like
      blobs when it could simply be a circle", and the user noticed the fill
      reaching further out than the border. One cause: the fill is dilated
      12 px after the contour and the border is not, so the fill bridges
      outlines the border keeps apart. Filed as B38.
- [x] B29: open a graph from a folder on the light theme, so its region is
      drawn, and seed a paper or two. Switch Zotero's Appearance to Dark:
      without touching the graph, the plot's paper and surround go dark, the
      region takes the dark palette's colour rather than keeping the light
      one, the seed discs take the dark seed colours, and the rail's legend
      matches the region. Back to Light, then to Automatic, which is the
      switch that failed on the fixed build: with the OS dark, the plot goes
      dark at once, not on the next hover or pan. Then, still on Automatic,
      switch Windows itself between light and dark: the plot follows. (Also
      covered by `test/zotero/graphThemeFlip.test.ts` for the paper on both
      pref switches, and `test/unit/citationGraphRendererThemeChange.test.ts`
      for the colours; the OS switch is the one no test drives.)
      Passes on 2026-09-11 on the 59ebeac build: "B29 is fixed."

- [x] B31: open a graph whose library has a folder with subfolders. A child
      row sits 14 px in from its parent with a thin guide line running down
      the levels above it; no leading spaces in any label. Untick one child:
      the parent's square turns grey with a white dash and the screen reader
      (Narrator, or NVDA if installed) announces the parent's checkbox as
      partially checked or mixed. Hover the parent's square, not its name:
      the child rows take a faint blue tint that leaves with the pointer.
      Passes 2026-09-13 on the 43dfe4b XPI on the three visual clauses:
      "Indentation, mixed state, and hove work". The announcement clause was
      skipped at the user's call rather than failed — firing up Narrator was
      not worth it — and the source answers it as far as source can:
      `graphKeyRail.ts:433-440` draws the mixed parent by setting
      `indeterminate` on a real `input[type=checkbox]`, which Gecko maps to
      the platform's MIXED state on its own, so no `aria-checked` is needed
      and a screen reader should say "partially checked". Walk it with NVDA
      if the rail's markup ever stops being a native checkbox.
- [x] B31: narrow the rail (drag the splitter) with a deeply nested folder
      that has a long name: the name ends in an ellipsis on one line, and
      the count stays at the right edge.
      Passes 2026-09-13 on the 43dfe4b XPI: "Ellipsis work. Counts work." So
      the indentation does not eat the width budget at the deepest level.
      One finding on the way, unrelated to the rail's layout and the more
      serious of the two: a folder renamed in Zotero keeps its old name in
      the graph until Zotero restarts — B58, filed with the cache that
      explains it.
- [ ] B31: click a folder's name to draw it as a region, then switch
      Appearance to Dark. The whole row is filled with the accent blue, the
      label and count are white, and the square is the region's swatch with
      a white edge; click a second folder and the two rows read as the same
      blue with different squares.
      **Fails** 2026-09-13 on the 43dfe4b XPI, on both themes rather than
      dark alone: the row is not filled with the accent blue, because the
      label-and-count container keeps an opaque background of its own and
      the blue shows only around it. Filed as B59, with the missing
      `.meristema-root` prefix that explains it. The rest of the check is
      unwalked until then — with the fill broken there is nothing to judge
      the white label, the swatch edge or the two-rows-one-blue clause
      against.
      Unblocked 2026-09-13: B59 is fixed, so this is walkable again on the
      next build. Walk the whole check, not just the fill — the white label,
      the swatch edge and the two-rows-one-blue clause were never reached.

Note for whoever walks this batch: D3's six unwalked checks are still open above, and they need rewalking on this build anyway, since D6 changes what a region looks like at every zoom. The six surface checks just above need an XPI built from `surface-fixes` (or from main once it is merged); the build from b2ff41d does not carry them.

- [x] D4: the plot toolbar reads File, View, Filter, Similar, Export,
      Refresh, then the search box, on both themes. Supersedes B21's order
      check
      Passes 2026-09-13 on the 43dfe4b XPI: "Pass".
- [ ] D4: open the View dropdown. Five rows with icon, name, one-liner; the
      two greyed ones (Reading plan, Who cites whom) read "Arrives with
      reading state / shared citers" in muted text and do nothing on click;
      contrast is readable on both themes
      **Fails** 2026-09-13 on the 43dfe4b XPI, on layout, not content: the
      five rows are all there but their descriptions overflow their
      containers and the rows' hit areas are offset by one, so Folder map
      cannot be reached. Both are B44, which this walk widened from the
      gallery to the dropdown. Note for the rewalk that the check's own text
      is now stale: it asks for three greyed rows, but citation hops shipped
      in Stage 3, so Cornerstones is live and only Reading plan and Who
      cites whom should be greyed — the two the user saw.
      Unblocked 2026-09-13: B44 is fixed, so this is walkable again on the
      next build, and the rewording is done — the check now asks for **two**
      greyed rows, Reading plan and Who cites whom, not three.
- [ ] D4: apply Overview. The chip reads "View Overview"; the tutorial card
      sits bottom-right of the plot, 300px, with chips "x year", "y
      citations", "size citations", "colour uniform", "labels author-year"
      and a last line "Seeds and collections are untouched."; narrow the
      window until the plot is under 400px and the card still fits
      **Fails** 2026-09-13 on the 43dfe4b XPI, on the last clause only. The
      chip and the card pass as written: "1, and 2 okay". Narrowing holds to
      400px and breaks below it — filed as B56. Tick the first two clauses
      when B56 is fixed and the narrow case is rewalked.
      Rewalkable 2026-09-14: B56 is fixed. On the next XPI the narrow clause
      reads differently from the check's text — the card holds its 300px down
      to a 328px plot, and below that it is compact by design: the view's
      name, × and "Don't show for this view again", no paragraph, chips,
      footnote or "Got it". Widening the window past 328px brings the whole
      card back. Walk it on both themes.
- [x] D4: change the y axis in the gear: the chip gains "(edited)"; reapply
      Overview from the dropdown: it goes back
      Passes 2026-09-13 on the 43dfe4b XPI: the chip gains "(edited)" on a y
      axis change, and reapplying Overview clears it and returns the axis to
      citations.
- [x] D4: open a folder graph of 300+ papers: the gallery covers the plot
      inset, the rail and toolbar stay usable behind it, the heading counts
      the visible papers; Start blank removes it and reopening the same
      graph shows no gallery
      Passes 2026-09-13 on the 43dfe4b XPI, all four behaviours: "Everything
      okay here". Judged on behaviour only — the cards' own layout is broken
      on this build (B44). The inset itself drew a design question, filed as
      D14.
- [x] D4: apply Folder map on a library with nested folders: one region per
      ticked subtree top, not one per child; with more than twelve, the card
      says "N regions, 12 colours"
      Passes 2026-09-13 on the 43dfe4b XPI on the clause that matters: "I
      applied it from the gallery, one region per subtree top", tried across
      the nested folders available (DOKwood paper, PhD). The twelve-colour
      clause could not be walked — the library has nowhere near twelve
      subtree tops — but it does not need a manual walk. The user was right
      to push back on the wording: the card reads "regions 2 folders", with
      no mention of colours, and that is correct. `graphViews.ts:434` always
      pushes `regions {n} folder(s)`, and line 435 pushes a second, separate
      chip `{n} regions, {swatchCount} colours` only when n exceeds the
      swatch count. At two regions there is nothing to say about colours. The
      capped case is covered anyway by
      `test/unit/graphViews.test.ts:492` asserts the "14 regions, 12 colours"
      string. Applied from the gallery because B44 makes the row unreachable
      in the View dropdown; that the gallery route works is itself recorded
      in B44 as the best clue to its cause.
- [x] D4: Save current as view: the panel's Captures list ticks Axes,
      Colour, Size, Labels, Regions, Filters; Explore reads unavailable;
      Scope is unticked and dashed; a name that matches "Overview" is
      refused inline; Copy JSON shows "Copied" in the toolbar
      Passes 2026-09-13 on the 43dfe4b XPI. The Captures list is ticked and
      disabled with Scope alone unticked, which is right: the spec calls the
      checklist read-only, a statement of what a view captures rather than a
      chooser. The Explore clause is stale and was not held against the
      build — Stage 3's spec retires the "not yet available" line and has the
      panel list the direction and depth it will save, so Explore being
      present is correct now; reword the check. The name refusal and Copy
      JSON both behave. Two findings on their presentation, filed: the inline
      warning is too small (B57) and the "Copied" confirmation is almost
      invisible (D15).
- [x] D4: the seven view icons at 16px in the dropdown and 28px in the
      gallery, both themes
      Passes 2026-09-13 on the 43dfe4b XPI: "Both themes okay" at both
      sizes, nothing muddy or off-centre. Two corrections to the check
      itself, neither a fault in the build. There are six icons, not seven —
      `uiIconService.ts:24-29` defines `view-overview`, `view-cornerstones`,
      `view-reading-plan`, `view-who-cites-whom`, `view-folder-map` and
      `view-user`, which is the five templates plus the saved-view flag the
      user counted. And the sixth has no 28px case to walk, because the
      gallery does not show saved views at all: see D16. Reword the check to
      six icons, and to 28px for the five shipped ones.
- [ ] B42: with Zotero closed, open the profile's
      `meristema-external.sqlite` in a SQLite tool and set one saved graph's
      `state` in `saved_graphs_v1` to `{"version":99}`; start Zotero and
      open that graph from Tools › Meristema › Open Saved Graph. The toolbar
      status reads "This graph was saved by a newer version of Meristema,
      so it is read-only here; Save as… keeps changes." on one line, fits
      the toolbar on both themes, and stays up (it does not fade like
      "Saved"); tick a folder, close the tab, reopen the graph: still the
      blank graph with the notice, the row untouched. File › Save asks for
      a name like Save as…; after naming it the notice clears and the new
      row autosaves
      **Skipped** at the user's call on 2026-09-13, not failed and not
      walked: "Skip it". Left unticked deliberately, since nothing was
      verified. The reasoning is the greenfield one the user already applied
      to the migration notice: what this checks is the notice an _older_
      Meristema shows a graph saved by a _newer_ one, and there is no newer
      version anywhere, nor any user but the author. The mechanics are not
      the obstacle: there is no `sqlite3` on the machine's PATH, but Node 24
      ships `node:sqlite`, so the row edit and its restore are a short
      script whenever this is worth doing. Re-offer it when a second version
      of the plugin exists in someone else's hands, and not before; the
      version-99 path is covered meanwhile by the case B42 added to
      `test/zotero/graphViews.test.ts`.
- [x] B41: select two items, Tools › Meristema › New Graph with 2 seeds: the
      x axis opens on Citation sequence (the seeded default) the first time.
      In the gear, set the y axis to Year; close the tab; open another
      seeded graph: the y axis is Year. Tools › Meristema › New Graph
      (library, no seeds) is unaffected by that change, and a gear change
      there does not touch the seeded graphs
      Passes 2026-09-13 on the 43dfe4b XPI on everything that could still be
      walked. The persistence holds: a y axis changed on one seeded graph
      with the tab closed opens the next seeded graph the same way. The
      separation holds too, and the user volunteered it as the better
      behaviour — Tools › Meristema › New Graph draws free in both axes and
      "stays like that independent on how the user edited it before". The
      first clause could not be walked and cannot be again without clearing
      the preference: "the first time" was consumed earlier in this same
      session, by the gear change in the D4 "(edited)" check and by Overview
      setting the axes. Seeing Publication Year is therefore the stored
      preference working, not a wrong default —
      `citationPreferences.ts:229-236` has `defaultFocusGraphAppearance` set
      `xMetric: "citation-sequence"` purely as the fallback, which any stored
      `focusGraphAppearance` overrides. To settle it, clear that pref in
      Zotero's Config Editor and open one seeded graph. Two notes for the
      rewalk: the path in this check is wrong — "New Graph with N seeds" is
      on the item context menu (`menuService.ts:191-196`), not Tools — and
      the M14 glance the handoff asked for could not be made, since the x
      axis was never on Citation sequence. The inheritance itself is disputed
      by the user: D17.
- [x] B41: on a seeded graph apply Overview from the View chip, Save as…,
      close the tab and reopen it from File › Open: the chip reads "View
      Overview" with no "(edited)"
      Passes 2026-09-13 on the 43dfe4b XPI: "It works, not (edited)
      anywhere". So the reopen rebuilds the graph without drifting from the
      saved view, and the answer the D17 watch was after is the reassuring
      one — a saved view wins over the inherited seeded appearance on load,
      which keeps D17 a disagreement with the design rather than a defect.
- [x] B22: select a paper in a graph. The detail pane's Advanced section is
      open on arrival, every row showing (dashes included), the twisty
      pointing down; clicking Advanced folds it, and selecting another paper
      opens it again
      Passes 2026-09-13 on the 43dfe4b XPI: "It works, Advanced opens every
      time". The fold does not stick across papers, which is the clause that
      mattered — an empty field now reads as a dash rather than as a missing
      row.
- [x] Stage 3: The dimmed hop rows' contrast in both themes.
      Passes 2026-09-13 on the 43dfe4b XPI: "the rail has lower opacity
      working", "both themes read fine".
- [ ] Stage 3: The progress line under a fast fill (a seed with a handful of
      citers), and the Fetch more state on a seed with hundreds.
      Part-walked 2026-09-13 on the 43dfe4b XPI, on a seed with hundreds; do
      not start it from zero. What holds: the line appears under the deepest
      open hop and reads `expanding · {n} left` with Stop; the row above
      reads `{shown}/{available} of {reported}` as specified; Stop flips the
      link to Resume and genuinely halts the fill — "now they are frozen".
      Still unwalked: the fast half (a seed with a handful of citers, where
      the line should appear, count down and then disappear), and the
      Fetch more state, which had not engaged by 627 of 800 parents left.
      Left open against D7, which is why: the numbers are spec-conformant
      but do not mean anything the reader can follow. Two further findings
      on the way, both filed: the Stop and Resume links paint a background on
      hover (B46) and the counts group thousands in the wrong locale (B49).
      A wart that is not a spec failure and had no verdict: paused, the line
      still reads `expanding · 627 left · Resume`.
      The fast half was walked 2026-09-13 on the same XPI and passes as
      written: "The line appears, counts down, and disappears." So the line
      stands itself down on completion and leaves no Stop behind. What keeps
      this check open is now only the Fetch more state, still unreached at
      B50's rate, and D7, which the fast walk fed a proposal into — the user
      would like a progress bar.
- [x] Stage 3: The Citation hop colouring with the Key in both themes.
      Passes 2026-09-13 on the 43dfe4b XPI: "hop colors work", "Colors work
      for both themes", and the Key agrees with the plot and the hop rows.
      One finding on the way, filed as D9: under this colouring the rail
      carries the hop colours twice, in the Hops rows and again in Color.
- [x] Stage 3: A hop-5 or hop-6 node under emphasis dimming still reads in
      both themes.
      Passes 2026-09-13 on the 43dfe4b XPI, at hop 5 with three rows
      selected in the item list: "They're still visible, faint but findable,
      on both themes". So the spec's open question is answered — the ramp
      multiplied by emphasis does not erase a deep node. Walked at hop 5,
      not 6, and hop 6 cannot be walked at all on this library: hop 5 already
      reaches 2026, so there are no hop-6 citers to draw (see D8). Two
      findings on the way, filed as D10: a tenth of alpha between adjacent
      hops is not tellable apart, and under Citation hop the depth is
      encoded twice.
- [x] Stage 3: The Cornerstones view on a real seed: the direction switch
      reads References, Hop 2 opens, the tutorial card lists
      `hops 2 · references`.
      Passes 2026-09-13 on the 43dfe4b XPI, all three parts: "It does
      switches from Citers to References", Hop 2 opened, and the card's
      chips read `x publication year`, `y citations`, `size citations`,
      `colour citations`, `labels author-year` and `hops 2 · references`.
      Two findings on the way, filed: the reference fill lands many papers
      with no metadata (D11) and a graph saved while stopped reopens
      fetching (B55). The card's own layout is sound: its transcription here
      came out interleaved with node labels from the plot, but the user
      checked and that was an artefact of copying the text, not the screen —
      "the card looks fine". The gallery and the View dropdown both break
      (B44); the tutorial card is the D4 surface that does not.
- [x] Stage 3: Panning during a fill: the plot does not grow because of the
      pan.
      Passes 2026-09-13 on the 43dfe4b XPI, walked with papers landing: "The
      pan doesn't grow the plot". The movement that does happen as papers
      land is B47's, judged separately.
- [x] Stage 3: The item tree stays responsive during a fill (column
      refreshes coalesced).
      Passes 2026-09-13 on the 43dfe4b XPI, walked with a fill running:
      "Not Zotero own interface, only the plot lags". So the coalescing
      holds and the cost the user reported in D8 is the plot's own, not
      something leaking into Zotero's panes.
- [x] Stage 3: The migration notice on a graph saved before the upgrade.
      Passes 2026-09-13 on the 43dfe4b XPI: a pre-upgrade graph opens showing
      Citers and the toolbar reads "Directions are now one at a time; showing
      Citers", as specified. Not walked, at the user's call: the third part,
      that saving as version 5 stops the notice recurring. Their reason is
      worth keeping for how much back-compat work to fund at all — "honestly,
      here it doesn't matter because this plugin is still greenfield (only me
      using and testing as we go)". The old graph also arrived with the view
      gallery up, which is B44's territory.
- [x] Stage 3: open a seeded graph, fetch to hop 3 or deeper, and check that
      hop-3+ nodes, labels and edges stay legible against both themes at the
      0.7 and lower opacity steps
      Passes 2026-09-13 on the 43dfe4b XPI, judged narrowly on legibility as
      the check asks: "I can read them". The same look produced a finding
      the check does not cover, filed as D13: the edges at this density stop
      carrying information at all.
- [x] Stage 3: during a fill (progress line reading `expanding · n left`),
      hover across the plot and drag-pan while pressing Stop, then a hop
      checkbox; the rail must not lose focus or flicker, and the progress
      line must keep updating per landing
      Passes 2026-09-13 on the 43dfe4b XPI: "It works". No flicker, no lost
      focus, the line kept updating. The remainder is the rate, not this
      check: "at Hop 4 it is painfully slow" — B50 and D8.
- [ ] B44: open a graph from a folder's context menu so the gallery greets
      it **in a tab, not a detached window** — the defect only exists where
      Zotero's own stylesheet applies. Every card's icon, name, paragraph and
      note sit inside its own rounded container; no text crosses a card's
      edge or overlaps its neighbour, and "Start blank" and "Import view
      JSON…" sit in their own cell rather than over the third card.
- [ ] B44: on that same graph open the View dropdown. All five rows read
      inside their own boxes, and the pointer lands on the row it is over —
      in particular hovering Folder map highlights Folder map, and clicking
      it applies Folder map. This is the half that was not cosmetic.
- [ ] B59: tick a folder in the Scope rail so its row is selected. The accent
      blue runs behind the whole row, through the folder's name and its
      count, not around a box containing them. With the pointer on the row,
      the hover is a neutral grey tint, not blue.
- [ ] B44/B59 together: walk the plot toolbar, the rail's buttons and the
      detail pane's tabs once. The fix lifted a height clamp from every
      plugin button at once, so what to watch for is the opposite of the
      defect — a control that has grown taller than its 28px slot. Nothing
      should have moved.
- [ ] B58: with a graph open on the real library, rename a folder in Zotero's
      collection tree. The rail shows the new name at once, without a reopen;
      if a hop fill was stopped, it stays stopped; and a graph opened after
      the rename shows the new name too. Open a graph from that folder's
      context menu and rename the folder again: the tab's title follows.
      Rename that tab yourself, rename the folder once more, and the tab keeps
      the name you typed.
- [ ] B60, closed as Zotero's own state: in a folder, click a paper in the
      list, then Ctrl+click it so it deselects, and note the dotted ring left
      on that row. Now repeat B30's walk — select a paper in the list, click a
      graph node whose paper is outside the folder, then click back into the
      list. The row keeps the same dotted ring as after the Ctrl+click, no
      more; ↑/↓ move from it in both cases. If the two look different, B60
      reopens.
- [ ] B46: on both themes, point at "+ Add seed", at Stop or Resume on a
      running fill, at Show all, and at the tutorial card's "Don't show for
      this view again". Each underlines and paints no box behind it; the ×
      beside a seed still goes to full ink and heavier.
- [ ] B57: open Save current as view… and type the name of a shipped view,
      such as Overview. The refusal reads at the same size as "Name" above
      it, amber in light and a lighter amber in dark, and is easy to read on
      both.
- [ ] B61: on both themes, point at an entry in the Key rail. It takes a faint
      grey tint, not a blue one. Click it to pin: it holds a light blue tint
      while pinned, and loses it when a click on the graph releases the pin.
      The rows keep their height.
- [ ] B62: open a graph saved before Stage 3 whose direction was `both` (or
      any graph whose toolbar shows "Directions are now one at a time;
      showing Citers"). While the notice shows, move any paper to the trash
      in the library. The graph redraws and the notice is still there. Then
      right-click the graph's tab › Move › Move to New Window: the new
      window's toolbar shows the notice, and trashing another paper redraws
      the window with the notice still there.

## Zotero suite

`npm test` launches the dev Zotero and runs `test/zotero`; the user has
said it may be run from a session (the backlog's "never start or stop
Zotero" rule is lifted for this). As of 2026-09-14 on main after B62 it is 85
passed, 0 failed; 2026-09-15 added one Citation hops case (a detached window,
B62), so a clean run is now 86, not yet seen whole. The Citation hops cases depend on Semantic Scholar's keyless
pool: when it answers 429 each expansion lands 0/0 after exactly 15 s and
those cases fail, which is the provider, not a regression (B50 has the
evidence; rerun later). On B58's branch it was 77 passed, 0 failed
(`folderChangesReachGraph.test.ts` added five cases). A suite
that moves Zotero's collection tree must put it back on the library before
erasing its fixtures: B58's suite did not at first, and the Citation hops
suite, which runs next, failed its version 4 sticky-notice case in both such
runs. The earlier count: the suite was fully green as of 2026-09-12:
59 passed, 0 failed (B42 added one case to `graphViews.test.ts` and a D4
follow-up another; the Scope rail's node-menu walk is settled, see B43).
D4 added its six cases in
`test/zotero/graphViews.test.ts`; a suite that clicks the plot of a fresh
graph must Start blank first, since D4's gallery overlay now covers that
plot until dismissed. Two cases used to fail on main before B2. "view 10" is
settled: the second action it saw on main was a duplicate Graph entry,
deleted in Stage 2, and the seed toggle beside "Find similar papers" is what
the overview offers now, which the case describes. "view 15" was backlog B11
and is fixed. `savedGraphMenu.test.ts` "lists the saved graphs on the first
showing" stays intermittent: on the F1 branch it failed two runs of four
with an error whose message printed as "undefined", and passed on main
once and on the B6 branch once; the test drives the real Tools menu, so a
focus or popup timing race is the first suspect. One failure there is not a
regression.

The unidentified 42-passed/1-failed run from 2026-09-10 is **still not
identified**. Three logged runs on `graph-region-shapes` were 43 passed, 0
failed each, so it did not reproduce — which is not the same as being closed.
Whatever it was is intermittent and rarer than one run in three, and the case
was never named. Keep the full log (`npm test 2>&1 | tee <file>`) on any branch
that touches the region code, and name the case the first time it comes back.
Nine logged runs on 2026-09-11 (B30's branch) did not bring it back: every
failure in them was B30's own case while it was red, and the final run was
44 passed, 0 failed.

The test Zotero runs the suites with its pane **locked**: its startup check of
the test database ("Checking database integrity…") is still running when the
scaffold starts the tests and its `unlockPromise` did not resolve in 45 s, so
`#zotero-pane-overlay` stays up over the tabs for the whole run. Synthetic
events dispatched on an element never notice, but a real pointer
(`windowUtils.sendMouseEvent`, the only way past the renderer's
`setPointerCapture`) lands on the overlay and reaches nothing. A suite that
needs a real click on the main window lowers the overlay first with
`Zotero.hideZoteroPaneOverlays()`, as `graphSelectionOutsideFolder.test.ts`
does and says why; a detached window (view 3) is not under it.

## Log

One line per session: date, what was ticked, commit range. Newest last.

- 2026-09-08: roadmap written; backlog copied into the repo (b921de8).
- 2026-09-08: B1 ticked; Save as and Delete use Services.prompt (native-prompt-dialogs, one commit after 798ea23).
- 2026-09-08: B2 ticked; the fill no longer needs the static entry, which Zotero creates only on the submenu's first showing (saved-graph-first-showing, one commit after da425fe). Zotero test added.
- 2026-09-08: B3 ticked; renaming a saved graph's tab already renamed its row (Zotero test proves the full menu path), the gap was a scratch view, whose rename now saves it under that name; Rename View dialog titled through Services.prompt (rename-saves-scratch-graph, two commits after 35e0e97).
- 2026-09-08: B5 ticked; the selection-sync walk-through now clears the list with Ctrl+click on the selected rows, since Zotero's list ignores empty-space clicks (one commit after 93fc2a6).
- 2026-09-08: F2 ticked; wheel zoom rate doubled, line and page delta modes normalised to pixels, per-event travel capped; pure `wheelZoomFactor` in graphViewport with unit tests (wheel-zoom-sensitivity, one commit after 31c08e4).
- 2026-09-08: F1 ticked; node menu leads with Open PDF / Open attachment / Open online from a pure `openPaperEntry`, best attachment opened on the host pane; view 12 extended (node-menu-open-paper, one commit after bca899a). Zotero suite: view 10 and 15 still fail as before.
- 2026-09-08: B6 ticked; the new item's citation update rebuilt the tab while Add to Zotero was still running, so the import's key landed on a dead view and the rebuilt seed stayed external. The pane now reports the import to the host, which writes the key into the instance's recipe and the live view and refreshes; pure `markExternalSeedImported` with unit tests; Zotero regression test walks the real tab (external-seed-import, one commit after 5b2ae03). Found B7 on the way; it is a deliberate guard that only logs, so it went to Filler as a decision.
- 2026-09-08: verification batch walked. B2, B3, F2 and F1 pass; B1's two
  checks pass as written but the dialogs are cramped and Delete paints
  white edges, filed as B8; an abstract-in-the-graph request filed as F4;
  B6 not walked yet.
- 2026-09-08: Stage 2 brainstorm and spec written
  (`2026-09-08-graph-scope-rail-design.md`, branch `graph-scope-rail`): the focus projection is retired for one model plus a
  pure visibility function, items become seeds, folders move into the
  rail as a checkbox tree, the toolbar's Graph menu becomes File and is
  mirrored in Tools, and the item menu becomes New Graph from N items
  plus an Add to submenu. Resolves D1 and D2, decides B4.
- 2026-09-08: Stage 2 spec reviewed by the user and approved; four review
  gaps closed, the largest being that subfolders must cascade because
  `collectionScopeIDs` already expands a selected folder through its
  descendants, so a non-cascading tree would have silently shrunk every
  saved folder graph on upgrade. B8 decided: keep the native dialogs.
- 2026-09-08: Stage 2 plan written
  (`docs/superpowers/plans/2026-09-08-graph-scope-rail.md`, branch
  `graph-scope-rail`): fifteen tasks, state version 2 and its migration first,
  then the two pure modules, the seed reach, the theme, the rail, the additive
  model, the seed panel, Remove from graph, the item-scope deletion, the File
  menu and its Tools mirror, the item menu, the Zotero tests, and the docs.
- 2026-09-08: B6's two manual checks walked and passed, on the plain external
  seed and on the DOI path. Stage 1's verification batch is closed; every
  Stage 1 item is now verified in Zotero.
- 2026-09-08: Stage 2 execution run, tasks 1 to 13 of the Scope rail plan
  landed on `graph-scope-rail` (eecc9b0..00bbba6), one subagent and one commit
  each, `npm run check` green throughout. The run stopped at the protocol's
  first `npm test`: 28 passed, 6 failed, four of them new — B6's
  `externalSeedImport` test and "view 13" may be real regressions, "view 5"
  and "view 12" are cases the plan never updated for the rail's seeding.
  Tasks 14 and 15 are not done.
- 2026-09-09: Stage 2 finished (`graph-scope-rail`, 4fb15de..): the focus
  projection is retired for one additive model plus a pure visibility order,
  items become seeds, folders move into the rail as a checkbox tree with
  Unfiled and Not in Zotero, Remove from graph and Show all arrive, the
  toolbar's Graph menu becomes File and is mirrored in Tools, and the item
  menu becomes New Graph from N items plus an Add to submenu. State version 2
  with a migration. Resolves D1 and D2, decides B4. The four failures the run
  stopped on are settled: the adopt pass was eating a restored seed selection
  (4fb15de), a state change queued on an animation frame never reached the
  host while nothing repainted, so an imported seed stayed external in the
  recipe (5754d3f), and views 5, 10, 12 and 13 described the pre-rail
  toolbar and menus (2f879ca). `npm test` is 37 passed, 1 failed; the one is
  view 15, which fails on main too and is now backlog B11. Merged to main
  fast-forward and pushed; XPI rebuilt after the last test run.
- 2026-09-09: Stage 2's manual batch walked. All nine checks pass as written,
  the migration included, so no saved work was lost. The walk found eighteen
  items. B12 to B22: folder colours dealt by rank, the dark theme's "+ Add
  seed", the seed row's ×, two clicks to seed, the centred Open list, the
  repeated icon, the fit-after-render jump, the folder menu's label, no
  feedback from a detached window, File fourth in the toolbar, and Advanced
  collapsed and padded with dashes. F5 to F9: node menu parity with the pane,
  seeding an external paper into a new graph, ticks that match what the graph
  was made from, saying why a paper has no metrics, and a tool for adding a
  DOI — the plugin already resolves one and never writes it back. D3 to D5: the colour
  system, graph templates, the logo. D3 comes first: B12 is a symptom of it.
  Handoff: `2026-09-09-walkthrough-findings-handoff.md`.
- 2026-09-09: D3 finished (`graph-colour-system`, 6606e07..): seeds got a
  dedicated six-colour palette validated against the ramp and against
  dichromacies; colour moved onto a pure ledger that hands a key the lowest
  free index and holds it while the key lives, closing B12; a folder left
  the node's fill and became a marching-squares region, drawn behind the
  nodes in that folder's colour; `"collection"` was retired as a node
  colouring in favour of `"uniform"`; saved state moved to version 3
  carrying `regions`, `swatches` and `seedSwatches`, with a version 2
  migration; the rail's folder row split so the checkbox means "in scope"
  and the row's body selects the folder as a region, capped at four with
  the oldest released. Task 10 walked the rail through Zotero's own menus
  (`test/zotero/graphFolderRegions.test.ts`) rather than the model, and
  `graphKeyRail.ts`'s row body now carries `data-collection-id` so the case
  finds a row without matching on a label. D3 ticked above; B12 struck.
- 2026-09-09: worth recording for whoever plans the next stage — finishing
  D3, `npm run check` stayed green throughout at 327/327 unit tests, and
  none of them noticed that `graphKeyModel.ts`'s `CATEGORICAL_COLOR_LABELS`
  still named the retired `collection` metric and carried no entry for
  `uniform`, the metric's own new default. Nothing in the unit suite builds
  a Key against a real default metric, so the gap was invisible there; the
  Zotero suite is what caught it, because building the Key on an ordinary
  graph throws `Unknown Meristema metric: uniform`, taking the rail down
  with it. The unit suite being green is not proof the product is coherent
  end to end — only a real Key build is.
- 2026-09-10: B11 ticked, and the Zotero suite is fully green for the first
  time (39 passed). The defect was in the case, not the pane: view 15 read
  "is a paper selected?" as "is there any `.cm-placeholder` in the detail
  body", and a selected paper with no metrics — every paper the harness's
  corpus builds — renders "No impact metrics for this paper yet." as a
  placeholder of its own. The case now reads the detail header, which carries
  the paper's title against "Paper details" for the empty state. That made
  the case's tail run for the first time and it failed too: it expected
  `addFocusItems` to report the selection to Zotero, on a spec sentence that
  named `revealItem` and five siblings Stage 2 retired. The survivor seeds
  through `activateFocusState`, which suppresses by design and says why, so
  the case now asserts that a command reports nothing and uses a real gesture
  — Escape on the canvas — as the control that the suppression is scoped
  rather than a flag left on. Both new assertions were watched red against a
  deliberately broken product. Spec updated
  (`2026-09-07-selection-sync-design.md`), no product code changed
  (selection-single-row-case, one commit after 4b8627d). B9 with B10 were
  taken next, while the manual batch waited: they need no manual check, and
  they had to land before a Semantic Scholar key was pasted.
- 2026-09-10: B9 and B10 ticked, one branch (`provider-rate-limits`,
  97f93ff..): the providers' own numbers came back and settled it. Semantic
  Scholar's authenticated grant is 1 request per second cumulative — the
  rate the keyless path already ran at — and OpenAlex returns 401 without a
  key, so it has no keyless path at all. "A key means go faster" had no case
  left in which it was true, so the branch is deleted rather than retuned
  and `providerExecutionPolicy` reads no preference. A 429 now gets three
  retries at roughly 1s, 2s and 4s with jitter that only lengthens the wait,
  through the `postponeProvider` delay the loop already applied, so no
  second wait competes inside `requestJSON`; `Retry-After` and the 15 s
  abandon are unchanged. Spec:
  `docs/superpowers/specs/2026-09-10-provider-rate-limits-design.md`. Pure
  logic, unit-tested end to end; nothing was added to the manual batch.
- 2026-09-10: D3's manual batch walked. Two checks pass — a removed seed leaves
  the other seeds' colours alone, and the rail's region legend reads in both
  themes. The other six are blocked or failed on one regression: a non-empty
  region selection blanks the plot, so a folder graph opens with no nodes and
  selecting a folder on a drawn graph loses them (B28, the next thing to do).
  Four more findings: the theme swap leaves the plot background light (B29), a
  node whose paper is outside Zotero's open folder selects some other item
  rather than clearing (B30), the rail does not show subfolders clearly (B31),
  and three two-way-selection features — graph → Zotero multi-select (F10),
  seed row ↔ seed node (F11), Zotero folder → region (F12). B11's own check
  passes.
- 2026-09-10: B28 fixed (`region-path-from-window`): `drawRegions` built its
  path with a bare `new Path2D()`, and the plugin's bundle runs in a scope with
  no DOM constructors, so the live plugin threw "Path2D is not defined" on the
  first frame that had a region to draw — and `draw()` latches `canvasError`,
  so the plot stayed blank for that renderer's life. The path now comes from
  `canvas.ownerDocument.defaultView` through a pure `regionPathFor`, unit
  tested with a fake window. The suite had been green throughout because its
  own doubles polyfilled `Path2D` onto the global scope, the one place a
  plugin never has one; that double now attaches it to the fake canvas's view.
  `npm run check` 354 unit tests green, `npm test` 42 passed, XPI rebuilt after
  the run. D3's six blocked checks are walkable again.
- 2026-09-10: D6 brainstormed and its design approved in conversation, spec not
  yet written. Two decisions: the outline is a centripetal Catmull-Rom → cubic
  Bézier fit inside `regionPathFor` (a polyline in data space re-facets as you
  zoom, a curve does not), and the falloff radius tightens past the fit zoom
  only, `min(spread * 0.06, spread * 0.06 * fitScale / scale)`, with the
  contour cache keyed on a quantised zoom bucket. The second knowingly reverses
  D3's data-space decision, on a reason its review never weighed: zoomed in, a
  data-space hull has its edge off-screen and stops saying which papers made
  it. Handoff: `2026-09-10-d6-region-curves-handoff.md`.
- 2026-09-10: D6's spec written, reviewed by the user over two rounds and
  approved, then planned; no code yet. Spec:
  `docs/superpowers/specs/2026-09-10-graph-region-curves-design.md`, which
  names the reversal against D3 and requires D3's own spec and
  `graphFolderRegion.ts`'s docstring to be amended in the same commit as the
  code. Three things the spec settled that the brainstorm had left open: the
  fit scale is computed per axis from the plot rect rather than latched from
  the last `fitView()` (a saved graph restores a transform and may never fit
  at all); the grid pitch must follow the radius down, which forces
  `folderRegionContours` to stamp each paper's compact support instead of
  evaluating every paper against every cell; and the tightening is floored at
  8x for work and memory, not for looks. Plan:
  `docs/superpowers/plans/2026-09-10-graph-region-curves.md`, eight tasks for
  subagent-driven execution on `graph-region-curves`.
- 2026-09-10: D6 finished (`graph-region-curves`, 3c14294..e80e5ee): a folder's
  outline is a centripetal Catmull-Rom → cubic Bézier fit inside
  `regionPathFor`, so it no longer re-facets as the reader zooms — a polyline
  in data space always does, whatever its vertex count, because the rasterizer
  flattens a curve in device pixels instead. The falloff radius became
  `regionFalloffRadius(spread, scale, fitScale)`: today's constant at and below
  the fit zoom, tightening in inverse proportion to the zoom past it, so the
  halo holds a constant size on screen and a territory pulls apart into its
  papers. The zoom enters through a quantised 12% bucket that joins each
  folder's contour cache key, so a pan still recomputes nothing and a slow zoom
  recomputes a handful of times; the radius is derived from the bucket rather
  than the raw scale, or the cache would be drawing one frame's contour at
  another frame's radius. `folderRegionContours` now stamps each paper's
  compact support into the grid instead of evaluating every paper against every
  cell, which is what keeps the field build flat as the pitch follows the
  radius down. This reverses D3's data-space decision past the fit zoom,
  knowingly and on the user's reason — zoomed in, a data-space hull has its
  edge off-screen and identifies nothing — and the reversal is recorded in
  D3's spec, in D6's spec and in `graphFolderRegion.ts`'s docstring rather than
  left as a contradiction. Spec:
  `docs/superpowers/specs/2026-09-10-graph-region-curves-design.md`. Plan:
  `docs/superpowers/plans/2026-09-10-graph-region-curves.md`. Four manual
  checks appended to the batch.
- 2026-09-10: the user walked D6's first two manual checks and reported two
  regressions, not passes. The zoom-dependent tightening reads as right in
  direction, but the offset was too large overall at every zoom, so
  `FALLOFF_FRACTION` drops from 0.06 to 0.04 (`graph-region-bspline`,
  one commit after 0f59ff6). Separately, the outline was wobbly: the first
  fit interpolated every marching-squares vertex, and those vertices jitter
  a little from one to the next as the grid quantises the true crossing, so
  the curve reproduced the jitter as visible wobble. `regionPathFor` now fits
  a uniform periodic cubic B-spline instead — the contour vertices become
  control points rather than points the curve passes through, so grid jitter
  averages away — landed already as `0f59ff6`. Both D6 spec amendments and
  the roadmap's manual checks are updated to match; neither check is ticked,
  since both changes need rewalking on a new build. `MAX_GRID_CELLS` raised
  from 500 000 to 1 200 000 to keep pace with the lower falloff's finer grid
  (worst case about 924 000 cells, up from about 410 000). `npm run check`
  green throughout, 371 unit tests, count unchanged from before this session.
- 2026-09-10: D6's follow-up round walked and handed off. The user reports the
  outline is better but still a bit wobbly and the offset could be smaller
  still. Both are open, and the handoff argues they are one question rather
  than two knobs: the fit that shipped is a **uniform** B-spline, so unevenly
  spaced marching-squares vertices get equal parameter intervals and curvature
  varies for reasons that are not about the shape — the "non-uniform" in NURBS
  is exactly what is missing — while the offset cannot be lowered again without
  either raising the grid budget a second time or loosening `pitch = radius /
5`, which is the same seam. Handoff:
  `2026-09-10-d6-followup-handoff.md`. One `npm test` run in that session
  reported 42 passed / 1 failed and the case was never identified; two later
  runs were 43/0.
- 2026-09-10: D6's follow-up brainstormed, specced and approved; no code yet.
  Spec: `docs/superpowers/specs/2026-09-10-graph-region-shapes-design.md`
  (4844663). The cheap check the last handoff proposed came back negative —
  raising `DEVICE_WELD` does nothing on a lone paper's ring, which has no
  bunched vertices to weld — and measuring the two candidates separately settled
  the diagnosis: removing grid quantisation jitter entirely, by Newton-refining
  every vertex onto the exact level set, improves smoothness by nothing
  (curvature sd 0.0511 to 0.0514), while evening the spacing does (0.0416). The
  wobble is a parameterisation artefact, so D6's spec and
  `graphFolderRegion.ts`'s docstring both name the wrong cause and are corrected
  when the code lands. The fix is to resample the ring to even arc length before
  the existing fit, not a non-uniform knot vector, which keeps the divisions out
  of the fit and away from the `canvasError` latch. The user's own idea carried
  the rest: a lone paper's contour is exactly a circle, so a folder partitions
  into components under "closer than 2R" — provably disjoint supports — where a
  singleton is drawn as an `arc()` and only a cluster gets the grid, all
  components sharing one lattice so every surviving loop is byte-identical at a
  given radius and pitch. That inverts the cost curve the offset was stuck
  behind (451k cells at max zoom becomes 21k, or 5k with the new constants),
  which pays for `pitch = radius / 3`, `FALLOFF_FRACTION` 0.03, a cell budget
  back down to 250k, and retires D6's 8x tightening floor. Handoff:
  `2026-09-10-region-shapes-spec-handoff.md`.
- 2026-09-10: D6's follow-up landed (`graph-region-shapes`): a folder is a set
  of shapes rather than a set of loops. `folderRegionContours` partitions its
  papers into connected components under "closer than 2R" — a decomposition,
  not an approximation, since past 2R the supports are disjoint — draws a
  singleton as the exact circle `R * sqrt(1 - t)` and gives only clusters a
  grid, with every component sampled on one shared lattice so that at a given
  radius and pitch every surviving loop is byte-identical to the one that
  shipped. That is asserted rather than assumed, and the assertion had to be
  rebuilt: the first version of it could not fail, because its fixture's
  cluster contained the folder's own min corner, so the folder anchor and a
  per-component anchor agreed. The contour golden's first triple is untouched
  and passed on the first green run. Each ring is now resampled to even arc
  length before the existing B-spline fit: the wobble was measured to be a
  parameterisation artefact and not the grid jitter D6's spec and the module
  docstring both named, and both passages are corrected — as is D6's own
  verification list, which went on calling the jitter case "the discriminating
  test" 380 lines below the correction. On the fitted curve the fixture's
  radial wander falls from 0.3198 to 0.1285. The decomposition inverted the
  cost curve the offset was stuck behind — a 300-paper folder at bucket 18
  goes from 451 k cells to 5 k — which paid for `PITCH_DIVISOR` 5 to 3,
  `FALLOFF_FRACTION` 0.04 to 0.03, `MAX_GRID_CELLS` back down to 250 000, and
  retiring D6's 8x tightening floor for a 30-bucket ceiling. D6's pan
  assertion, which passed whether or not the contour was recomputed, is
  replaced by a real `folderRegionContours` call count through `mock.module`
  (`test:unit` gains `--experimental-test-module-mocks`). `npm run check` 389
  unit tests green; `npm test` run three times with the logs kept, 43 passed
  and 0 failed each. Spec:
  `docs/superpowers/specs/2026-09-10-graph-region-shapes-design.md`. Plan:
  `docs/superpowers/plans/2026-09-10-graph-region-shapes.md`. Three manual
  checks replace D6's first two.
- 2026-09-11: the manual batch walked on the b2ff41d build. D3's six rewalked
  checks all pass, including the version 2 migration; D6's first two shapes
  checks pass; the console check passes with one unattributed line. The walk
  found six items: F14 (the user wants the four-folder region cap gone), B32
  (a deep purple swatch on the dark theme), B33 (halos deform toward each
  other and meet in a point before merging — the kernel's hard edge, not the
  fit), B34 (a 300+ paper folder lags on pan at maximum zoom; later, by the
  user's choice), B35 (the item menu's "fetches online" hint misleads and
  the label should say the items become seeds), and B36 (one "Uncaught (in
  promise) undefined", filed because the intermittent savedGraphMenu failure
  printed "undefined" too). The plugin-manager ⋯ menu order the user queried
  is Zotero's own add-on manager, recorded under the backlog's answers. The
  zoom-bucket check is reworded after the user asked what it meant, and
  stays open. B30's check on this build is red as predicted; the fix is on
  the unmerged branch.
- 2026-09-11: `FALLOFF_FRACTION` 0.03 → 0.025 on the user's instruction from
  the walk (`falloff-0025`, one commit after 3bb41f2). The zoom-rule unit
  tests were retargeted first and watched red; the shapes spec records the
  move and rescales its cost table. One manual check appended: the new offset
  reads right or says which way to move.
- 2026-09-11: the zoom-bucket check walked and passed on the 09a29ac build;
  no deadband. D6 is fully verified except the new 0.025 offset check, which
  needs the fresh build. Handoff: the temp directory's
  `2026-09-11-post-walk-handoff.md`.
- 2026-09-11: B30 ticked (`clear-zotero-selection-when-absent`, one commit
  after b2ff41d, unmerged for review). Reproduced first, on the plugin's own
  path: a new Zotero-suite case opens a graph from Tools › Meristema › New
  Graph with a folder open and its paper selected in the list, clicks a node
  whose paper is outside that folder with a real pointer, and watches the
  boundary through a spy on `itemsView.selectItems`. The plugin asks Zotero for
  exactly the outside paper's ID; Zotero 10.0.2's `selectItems` finds no row,
  returns 0 and moves nothing, so the "other item" is the one the list already
  had — neither side substitutes. `selectListed` now takes 0 as "clear": it
  sets the binding's current set to empty first and calls the tree's
  `selection.clearSelection()`, so Zotero's echo of the clear is already
  current and reaches no graph, and the clicked node keeps its selection
  (an echoed empty selection would otherwise have cleared it through
  `applyLibrarySelection`). `ItemsTreeLike` gains `selection`; the spec's
  window-service passage says the same. Getting the case to red took four
  diagnostic runs: a constructed `PointerEvent` throws in `setPointerCapture`
  before the renderer selects, and a real pointer landed on Zotero's locked
  pane overlay (recorded under "Zotero suite" above). Gate green at 393 unit
  tests (`npm run check`); the suite 44 passed, 0 failed on the final run,
  logs kept. One manual check appended.
- 2026-09-11: the surface batch, one branch (`surface-fixes`, one commit after
  b2ff41d, unmerged for review): B13 the rail's links get a `--cm-link` token
  that is the accent on light and a readable light blue on dark; B14 the seed
  row's × is a glyph, hover and focus change its ink and weight and paint no
  shape; B16 the Open list's buttons get `text-align: start`; B17 the Tools
  submenu keeps its icon and its rows — Open, Save, Save as…, the saved-graph
  rows — drop theirs, the flat context menus untouched; B21 File leads the
  plot toolbar; B19 one folder's entry reads "Create a new graph", several
  keep "New Graph from N Folders", and the unused `$graph` argument is gone.
  Nothing here is reachable from the unit suite (CSS, a locale string, XUL
  attributes and DOM order), so no test was added; `npm run check` stays green
  at 393. Six checks appended to the manual batch. `npm test` not run.
- 2026-09-11: B24 ticked (`swatch-read-write-split`, one commit after b2ff41d,
  unmerged for review). The two colour readers no longer write back: the
  `swatches` and `seedSwatches` variables became `SwatchLedgerStore`s in
  `graphSwatchLedger.ts`, whose `peek` is `allocateSwatches` without the keep
  and whose `ensure` is the keep. `regionsForRenderer` and `seedColorsFor`
  peek; a new `ensureSwatchesFor` runs before `notifyStateChange` in
  `toggleRow`, `selectRow`, `applySeedProjection` and `clearSeeds`, so every
  allocation and release that lands in a ledger is the one `getState` reads
  next. `applyState` restores through the store and does not ensure: it does
  not always notify, and a peek on the restored ledger paints the same colours
  the next ensure lands on. B25's `categorySwatches` is untouched — that
  ledger lives on the renderer, not in these stores, so the split and the
  review branch meet only at the two declaration lines. Watched red on the
  store (five cases: a peek leaves the ledger alone, an ensure changes it,
  the two agree, an ensure releases, a restore reads through). Gate green at
  398 unit tests (`npm run check`); `npm test` not run.
- 2026-09-11: B23 ticked (`prune-deleted-regions`, one commit after b2ff41d,
  unmerged for review). `regionsStillInLibrary(regions, collections)` in
  `graphScopeRailModel.ts`, beside `nextRegionSelection`, keeps the IDs whose
  collection the snapshot still lists, in their order; `applyState` restores
  `regions` through it. That is the only seam needed: the snapshot's folders
  never move for the life of a view, so a deletion always arrives as a new
  snapshot and a fresh `applyState(getState())`, and a graph opened from a
  folder is validated by `openCollections` already. Watched red first (three
  cases: the ID is dropped, survivors keep their order, the slot it held is
  free for a fourth pick). Gate green at 396 unit tests (`npm run check`);
  `npm test` not run.
- 2026-09-11: B27 and B25 ticked, one branch (`swatch-bounds-and-category-ledger`,
  two commits after b2ff41d, unmerged for review). B27: `swatchIndexFor` was
  never checked against the palette, so a hand-edited or shrunk-palette index
  indexed past `theme.categorical.swatches` and canvas kept the previous draw's
  stroke for the `undefined` assignment; `categoricalSwatchAt` now returns the
  Other tone for any index outside the pool and both colour reads use it. B25:
  the category ledger becomes `GraphViewState.categorySwatches`, a field of its
  own beside `swatches` (the keys can collide, so no shared map), restored
  into the renderer by `applyState` through a setter that drops the cached
  assignment, and read back by `getState`; no state version bump, since the
  parser yields an empty ledger for a record without the field. Both watched
  red first. `npm run check` 401 unit tests green; `npm test` not run, XPI
  from b2ff41d untouched. B26 left alone as instructed — whether a region's
  hull should follow the search box is the user's call.
- 2026-09-11: the five review branches merged into main on the user's word,
  B30 first, then `surface-fixes`, `swatch-read-write-split`,
  `prune-deleted-regions` and `swatch-bounds-and-category-ledger`. Each
  merge conflicted only in this file and the backlog (both sides kept) and,
  for the last two, in `applyState`, resolved so the pruned regions, the two
  ledger `restore` calls and the category-ledger handoff all stay.
  `npm run check` green after each merge, 409 at the end. `npm test` found
  one real break: B21 put File ahead of Export, and the File popup shares the
  `cm-export-menu` class, so view 6's `querySelector` clicked the File button.
  The test now names the Export popup with `:not(.cm-graph-menu)`; suite
  44/0. Branches deleted; XPI built from this merge.
- 2026-09-11: the user walked the 8ee1467 build. B13, B17, B21, B19 and the
  D6 0.025 offset pass. B30 fails on an online-fetched node (the view
  reported null and the window service read that as a deselection): the
  callback now carries the node and the binding gains `clearListed`. B14
  fails as a dark box on the × itself: paperDetail.css's generic button
  hover at (0,3,1) beat the × rule at (0,3,0); the × rules now carry the
  tag. B16 could not be read because the File popup opened under the rail
  (B37, new): `.cm-graph-menu` anchors left. Three checks re-appended.
- 2026-09-11: B35. The user picked "New Graph with N seeds" for the item
  entry and chose to drop every acceltext hint, so `MENU_HINTS`, `menuHint`
  and `applyHint` leave `menuService.ts`; only the Add to submenu's "adds as
  seeds" stays. The message id is unchanged, so `typings/i10n.d.ts` did not
  need regenerating. `npm run check` 410 green.
- 2026-09-11: F14 and B32. The cap: `MAX_GRAPH_REGIONS` and the three
  `slice` calls go, `nextRegionSelection` loses its `cap` parameter, three
  tests retargeted red first. The user answered "we need more colours" to
  wrap-or-Other, so each theme's categorical list grows from eight to
  twelve; the additions were searched for over the sRGB cube against the
  validator's floors (plus every seed and state colour) rather than chosen,
  keeping the same four families in both themes. B32 was not a region
  swatch: the user pointed at the fourth dark seed, `#442b87`, 12 lightness
  points off the dark paper, which the validator never banded because only
  categorical swatches had a lightness test. A new test floors every seed
  and swatch at 20 points from its paper (the shipped minimum was 23.5), went
  red on that seed alone, and the seed is `#6c448c`. `npm run check` 411.
- 2026-09-11: B33. Rendered two papers at five separations under the shipped
  kernel, Wyvill's cubed one and the squared one, each smooth kernel with
  its support widened so a lone disc keeps its size; the user chose the
  squared kernel (support ×1.31, versus ×1.56 for the cubed). The stamping
  loop squares the bump, the disc radius is `R·√(1−√t)`, the isolation
  radius quoted in the docstrings is `1.541R`, and `regionFalloffRadius`
  carries the widening so the renderer is untouched. Tests: the disc golden
  moved to the new formula, a new case pins two papers 1.7R apart giving two
  loops and no islet (red under the old kernel, which summed to 0.555 at
  their midpoint), the five three-paper cluster fixtures take radius 13 so
  they still merge, and the accumulation golden was retaken (61 vertices).
  The D6 shapes spec carries a dated note. `npm run check` 412.
- 2026-09-11 (night): B29. A probe through Tools › Meristema › New Graph
  flips the appearance pref without touching the graph and reads the plot's
  pixels back (`test/zotero/graphThemeFlip.test.ts`). Light → Dark repainted
  on the 860701e build every time; the 2026-09-10 sighting was on 259cd17
  with a region selected, where B28's throw had latched `canvasError` and
  `draw()` returns before painting while latched, so that flip's redraw
  never ran. Light → Automatic on this dark OS failed two runs in three: at
  the pref write a fresh `prefers-color-scheme` query in the chrome still
  said light, 1.5 s later it said dark, and nothing fires in between. The
  pref observer resolves at the write. Gecko's `-moz-system-dark-theme`
  reads the OS setting directly and said dark throughout, so Automatic now
  resolves through it and the observer listens on it too; the chrome query
  is the fallback where the feature is unknown (`media === "not all"`). A
  third thing the code showed: region and seed colours are strings read
  off the renderer's theme when pushed, and a flip pushed nothing, so they
  kept the old palette. The renderer now re-resolves in the pref observer
  and calls a new `onThemeChange` before the redraw; the service re-pushes
  regions there (`refreshScopeRail`, which also repaints the rail's legend)
  and seed colours. `setRegions` grew the `draw` flag `setSeedColors` has.
  Unit cases: the resolver under each pref value with a lagging chrome
  query, and the renderer's callback running with the theme already dark
  with the redraw carrying the colour handed back. `npm run check` 417.
- 2026-09-11: B31 ticked (`scope-rail-folders`, plan
  `2026-09-11-scope-rail-folders.md`). The Scope rail's folder rows became a
  tree: indent as padding with a guide line per level, a filled square
  (`scopeSquare`) in place of the checkbox carrying the mixed, hover and
  selected states, and a selected row takes Zotero's accent fill with a
  white label, count and square edge; the click handlers did not change.
  Building it exposed a pre-existing bug on `main` since fd5019c (the
  commit that gave the rail its Scope section): the hidden checkbox of a
  mixed parent was built both `checked` and `indeterminate`, so a native
  click toggled it to unchecked and hid the whole subtree instead of
  committing to shown; it is now built unchecked and `indeterminate`, so the
  click commits to shown, which the new case "draws the parent mixed when
  the child is unticked, and refills both on the parent's click" exposed.
  Both final `npm test` runs: 51 passed, 0 failed, logs kept.
- 2026-09-12: B39 ticked; the two custom properties nothing read since B31
  are gone from `graphThemeCustomProperties` (one commit after 908d94a).
  `npm run check` green, 423 unit cases; the Zotero suite was not run, since
  no Zotero-side code reads the pair.
- 2026-09-12: B40 filed and ticked (ca99e31 and the next commit). The
  emitter is down to `--cm-surface-panel`; the ring case that only checked
  publication is replaced by one that pins the whole list in both schemes.
  `npm run check` green; the Zotero suite was not run, since no Zotero-side
  code reads the dropped tokens.
- 2026-09-12: D4 brainstormed and specced as `2026-09-12-graph-views-design.md`
  from section F of the Claude Design handoff `design_handoff_feature_artboards`
  (boards 3a to 3c), read through the browser; the package itself is still to
  be dropped into `docs/`. Decisions: all five views ship now with three
  greyed, the chip goes after File, gallery cards use drawn icons. Not ticked:
  the tick is for the build.
- 2026-09-12: D4 built. Views: five shipped (two live), chip after File,
  tutorial card, gallery once per graph, saved views in the profile, JSON
  import and export. Graph state is version 4 (`view`); version 3 records
  keep their regions. B41 stays open. Unit 451 cases; Zotero suite 57 passed,
  0 failed, two runs, logs kept. Runs 1 and 2 (54/2) exposed two old cases
  broken by D4, fixed in 4883c4b: the hidden View dropdown was built at
  mount, putting the Cornerstones summary's "Seeds" into the toolbar text the
  Scope rail case scans (fixed by building the menu only on open); and the
  new gallery overlay covered the plot where
  `graphSelectionOutsideFolder.test.ts` clicks (that suite now dismisses the
  gallery with Start blank first). Run 3 (53/3) had a three-case timing
  cascade in the Scope rail's canvas node-menu walk (hover then right-click
  hit-test on a settling layout); it did not recur in runs 4 and 5, so it is
  recorded as observed but not identified as D4's and not closed; the final
  review's four runs on 2026-09-12 hit the same three cases twice more (two
  runs green at 57, two red at 55 and 54), so it is a live flake, not a
  one-off, and worth a backlog entry of its own. The re-review diagnosed it
  as the walk's own right-click timing, not D4's gallery: see B43. Worth a
  line for Task 2: the plan's pseudocode said a missing metric falls back to
  "the first metric with data"; the gear actually falls back to Free (axes)
  or Uniform (size, colour) and gates the four categorical colours on node
  data, and the rule now lives once in `graphLayoutAvailability.ts`
  (`normaliseLayoutFor`, `colourOptionHasData`), which the gear calls.
  Commit eda7220 ran prettier over the D4 spec and plan, which had been
  committed unformatted.
  The final review settled the spec's "pre-filled" save panel: editing a
  saved view renames and re-words it and never re-captures the graph, so a
  gear change made after the view was applied cannot be written into it by
  opening Edit. The three missing-value filter chips ("filter missing year
  hidden" and its two siblings) are new copy, not in the design package.
- 2026-09-12 (after D4, no manual check needed): B43, B42 and B41 fixed on
  their own branches and fast-forwarded to main, in that order. B43 was a
  test-only fix and its handoff diagnosis was wrong; the walk's own probes
  found the real cause (fractional grid points truncated by the synthetic
  `contextmenu`; details in the B43 entry). B42's new copy, verbatim: "This
  graph was saved by a newer version of Meristema, so it is read-only here;
  Save as… keeps changes." — the toolbar status gained a `sticky` option for
  it, since the 2.5 s fade of "Saved" would have hidden the reason autosave
  is off. B41 reads the focus appearance record for a graph that opens
  seeded; its second-order effect (a view applied to a seeded graph comes
  back without "(edited)") follows from reading the same record the view's
  `setLayout` wrote, with one edge left: `setFocusGraphAppearance`
  normalises a citation-sequence axis to a linear scale, so a view that
  paired that axis with a log scale would reopen "(edited)". `npm run check`
  green, 459 unit cases; Zotero suite 57 passed, 0 failed on four runs after
  the B43 fix, 58 passed on two runs each for B42 and B41 (logs
  `b43-fix-run1..4.log`, `b42-run2..3.log`, `b41-run1..2.log` in the
  session scratchpad; the earlier `b42-run1.log` was red only through B43's
  walk, with B42's own case passing). The three D4 minor follow-ups went in
  as small commits on main afterwards: the outer `let view` in
  `graphViewService.ts` is `activeViewRef`, `GraphViewsMenu` no longer
  exports `refresh`, and `graphViews.test.ts` covers importView's "not a
  view" alert by redefining `Services.prompt` around the click (59 cases;
  `final-run.log`). XPI rebuilt last.
- 2026-09-12 (evening, after the handoff): B22 ticked; the Advanced
  `<details>` in `paperDetailView.ts` now sets `open` on creation,
  twisty kept, every row kept as decided on 2026-09-09. Pinned by an
  assertion in the Zotero suite's detail-pane case (`graphViewVisual.test.ts`),
  written red first (`b22-red-run.log`) then green (`b22-green-run.log`) in
  the session scratchpad. One check in the manual batch. XPI rebuilt last.
- 2026-09-12 (late): the user decided the `design_handoff_feature_artboards`
  package is not to be dropped into `docs/`; the D4 spec is the record. The
  four `// draft` tutorial paragraphs in `graphViews.ts` are no longer
  waiting on it. Stage 3 (citation hops on demand) is the next item, to be
  opened with a brainstorm in a fresh session.
- 2026-09-13: Stage 3 (citation hops) implemented and reviewed on
  `citation-hops`, 35 commits from main's `c1ed6f6` through `252f5c9`
  (fifteen plan tasks, one product-defect fix for a one-provider References
  expansion, the whole-branch review's fix wave, and three assertion fixes
  from its re-review). Merged to main fast-forward the same day, branch
  deleted; pushed to origin the same morning (c4d6ddf..f944e02), so the
  row above is ticked. `npm run check` green, 530 unit cases, 0 failed. Zotero
  suite 68 passed, 0 failed on the last three runs (the timing check
  included, the last one captured in full); the new suite is
  `test/zotero/graphCitationHops.test.ts`, seven cases plus two repeats. Two earlier runs in the same wave failed —
  one 65/3 whose failure text was not captured, one 67/1 where the
  version-4 migration-notice case failed once — and neither is resolved, so
  it stands as a live flake signal, not closed. Graph state is version 5
  (`hops` replaces `explore`); version 3 and version 4 records migrate, and
  a `both` direction shows the notice "Directions are now one at a time;
  showing Citers". One open decision for the user: the save panel's
  Captures row for an existing view whose `explore` is null shows nothing
  (shipped), where the plan's text called for falling back to the live hop
  state; the shipped form matches what the save actually stores. Ten checks
  appended to the manual batch above (eight from the spec, two from the
  whole-branch review). XPI rebuilt last.
- 2026-09-13: architecture pass on `architecture-pass` (17f9015..72868aa
  plus this line): M14 fixed by map, seed marks as one record, the fill as
  `graphHopFillRunner.ts`, the review Minors M3/M8/M11/N4 and three unused
  renderer methods. 550 unit; suite 68/0 on the final head after one
  provider-shaped 67/1 (ledger). XPI rebuilt last. Merged to main
  fast-forward at 2ba2696 and pushed the same day. Handoff:
  `C:\Users\nunesd\AppData\Local\Temp\2026-09-13-architecture-pass-handoff.md`.
- 2026-09-14: B58 ticked; spec approved with the tab title folded in (no
  B62). Folder changes rebuild only the folder lists; open graphs refill
  their lookups in place without remounting; folder graphs' default tab
  titles follow a rename (folder-changes-reach-graph, 36881cb..7fcdfaf plus
  this line). 562 unit; suite 77/0 after this branch's suite was taught to
  reset the collection tree. One manual check added. XPI rebuilt last.
- 2026-09-14: B60 ticked without code, on the user's call: Zotero's tree has
  no unfocused state, and the dotted ring is the one its own Ctrl+click
  deselect leaves. One manual check added; XPI untouched.
- 2026-09-14: B56 ticked, direction A (compact below a 328px plot) chosen by
  the user from a design canvas of three. 562 unit. The new D4 case failed
  red, then passed on both runs with the fix. The rest of the suite was not
  clean on those runs: 67/7 with the fix. A control run with the fix set
  aside came out 65/8, with the same Citation hops, B58 before-all and
  saved-graph reopen failures, so they are not B56's. Every provider-bound
  step slowed at once (the seed import 1s → 18s, a hop 2 Refresh 1.7s →
  108s) while every drawing-only case kept its time. Provider calls from the
  shell answered in under a second, so the cause is unconfirmed; rerun on a
  rested network before reading these as regressions. The D4 narrow check
  is rewalkable. XPI rebuilt last.
- 2026-09-14: B46 and B57 ticked, option A of each, chosen by the user from a
  second canvas. B46: the four text-link buttons (three in the rail and the
  tutorial card's dismiss link) share one tagged hover rule, an underline and
  no box. B57: the name warning is 12px in a new `--cm-warning-ink`, lighter
  in dark. Four cases in `hostButtonHeight.test.ts`, red then green: that
  file 9/9, run alone. The full suite was not rerun: its provider-bound cases
  were still timing out (see the B56 line), and these are hover and warning
  rules in `graph.css`, measured in the main window. Two manual checks added.
  XPI rebuilt last.
- 2026-09-14: B49 ticked without code, on the user's call: Zotero formats its
  own counts with the same locale-less `Intl.NumberFormat`, so the rail
  matching it is the consistent choice. XPI untouched.
- 2026-09-14: suite health, B61, B50 measured, B62 found and fixed. The
  first full run was 81/1 (folder regions' before-all, which passed in every
  later run and was not pinned down). The hop cases' failures through the
  day were Semantic Scholar answering HTTP 429 to the keyless test profile,
  measured with a `Zotero.HTTP.request` wrapper and a shell poller in the
  same seconds; written into B50 with no fix, on the user's call. B61 ticked,
  hover fixed and, on the user's call, the pinned tint too, which the
  measurement found lost: two cases red then green. B62 filed from the one
  failure of an 84/1 run and fixed on the user's call: one case extended red
  then green (a first fix stayed red and was reverted). 562 unit; suite
  85/0. Two manual checks added. XPI rebuilt last.
- 2026-09-15: B62's detached follow-up, test first on the user's call. Case 8
  in the Citation hops suite moves the version 4 tab to a new window and
  remounts it: red (the notice was already gone after the move), then green
  with `announceMigratedDirections` in `renderDetachedWindow`. The Citation
  hops suite alone was 6/4 red and 7/3 green; the three failing in both runs
  are the hop fills reading 0/0 after 15 s, Semantic Scholar's keyless 429
  (B50). The full suite was not rerun while those refuse. The manual B62
  check gained the move. XPI rebuilt last. Next: B50 with D8, brainstorm.
